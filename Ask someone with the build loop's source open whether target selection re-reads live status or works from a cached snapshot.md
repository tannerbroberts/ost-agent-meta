---
type: AssumptionTest
source: 'agent-ideated:2026-08-19-unattended-sweep'
created: '2026-08-19'
evidence: assertion
lane: humans-required
---
#AssumptionTest #unvalidated #evidence/assertion

Threshold: the answer names where the target list is built (once per firing vs. re-read at commit) and whether a status change written after list-build but before commit would be seen. Either answer settles whether this solution or its "missing filter" sibling is the actual fix.

A person outside the building is the measurement here: Answering this requires reading the build loop's source and its selection/commit sequence in the product repository; this unattended maintenance pass holds no repo-read grant, so a person with that access must check and report back.

## Issues
- 2026-08-22 2026-08-22 (unattended sweep, repo sight held) — THE REPOSITORY ANSWERS THE MECHANICAL HALF. The node's stated reason for the humans-required lane is "this unattended maintenance pass holds no repo-read grant"; this pass held `ost_read_repo`, so that reason no longer applies. WHERE THE TARGET LIST IS BUILT: `buildableSolutions(tree)` in `src/eval/buildable.ts` is pure — it takes the tree as an argument, builds `indexByTitle` once, and returns titles. It caches nothing across calls. `examples/automation/build-pass.sh` invokes it once per firing (`node "$CLI" buildable --vault .` into `$STATE/instrumented.txt`), then puts every line back through a SECOND fresh CLI invocation (`node "$CLI" buildable "$sol" --vault .`) before believing it. Each invocation is a separate process re-reading the vault from disk, so there is no stale in-process snapshot: a status change written between the two would be seen by the second. All four state files (`candidates.txt`, `buildable.txt`, `pending-verification.txt`, `instrumented.txt`) are truncated at the top of every firing, so nothing is carried between runs either. THE REAL STALENESS IS ELSEWHERE, AND IT IS STILL LIVE. `permitFrom` decides on RECORDED observations — `observedRed(t) && !observedGreen(t)` — and never re-runs the command. `src/eval/buildable.ts` names this hazard explicitly in its own header: "A recorded observation is a fact about the past, and a permit is a claim about now... the loop deliberately never re-observes an instrument it has already seen red." It cites the 2026-08-06 incident where a full model pass was spent on a solution whose instrument had been green for 17 minutes. The module ships `confirmPermit` as the stated answer, and documents that it is "deliberately NOT folded into buildPermit" — it is opt-in, and the caller about to spend something must call it. OBSERVED ABSENCE, the part worth a human's attention: `confirmPermit` is invoked NOWHERE in `build-pass.sh`. The script runs `verify` only over `buildable --pending` (tests never yet observed, capped at 8), and otherwise calls `buildable <sol>`, which routes to the pure `permitFrom`. So the exact hazard the module was written to close is open on the loop's actual path — a permit that went green outside the loop still reads as cleared. SO THE ANSWER TO THE NODE'S QUESTION AS WRITTEN: status is re-read live, per firing, per title; but the RED OBSERVATION that mints the permit is a cached fact that nothing on this path revalidates. Those are different questions and the node conflates them; a human may want to split it. WHAT IS STILL A HUMAN'S: whether the loop should call `confirmPermit` is a design decision, and re-laning this test is `ost-agent lane "Ask someone with the build loop's source open whether target selection re-reads live status or works from a cached snapshot" --set compute-only`. NOT ACTING: no lane changed, no result recorded, no instrument set. Source: first-party `ost_read_repo` of `src/eval/buildable.ts` and `examples/automation/build-pass.sh`. Nothing executed; rung unchanged.
- 2026-08-22 2026-08-22 CORRECTION to the annotation above, same pass, before reporting — WITHDRAW THE "OBSERVED ABSENCE" CLAIM. The annotation above states: "`confirmPermit` is invoked NOWHERE in `build-pass.sh`", and builds on it the conclusion that "the exact hazard the module was written to close is open on the loop's actual path." THAT CLAIM IS NOT SUPPORTED BY WHAT THIS PASS ACTUALLY READ, and it should not have been stated as an observation. WHY IT IS WITHDRAWN: `ost_read_repo` returned `examples/automation/build-pass.sh` with `"truncated": true`. A follow-up probe reports the file at 49,506 bytes; the read cut off partway through the `BUILD_COUNT -eq 0` branch, so well under half the script was seen. An absence claim over a file read in part is not an observation at all — it is the same vacuous-negative shape this vault's own sweep guard exists to refuse ("a sweep that read nothing is a failure, not a clean run"), and the sweep here read some and reported as though it had read all. EVIDENCE POINTING THE OTHER WAY, which makes this likely wrong and not merely unproven: the script's own header comment describes the firing condition as "Claude is invoked only when the gate has already let something through AND that instrument still fails today", and it names the preflight as "the candidate's own instrument re-run once against the repository". Re-running the candidate's instrument to find out whether the red still holds IS what `confirmPermit` does. So the mechanism the annotation above reported as missing is described as present in the part of the file that was read, and the section that would implement it is in the part that was not. WHAT SURVIVES FROM THE ANNOTATION ABOVE, unaffected by this: the answer to the node's actual question. `buildableSolutions` is pure and caches nothing; the shell re-invokes the CLI as a fresh process per firing and again per title; all four state files are truncated each firing. That part rests on `src/eval/buildable.ts`, which was read in FULL and not truncated, plus the portion of the script that was read. The distinction the annotation draws — status is re-read live, while the red observation that mints the permit is a cached fact — also stands, since it follows from `permitFrom` alone. WHAT IS NOW OPEN AGAIN, and is the thing to check: does the build loop confirm a recorded red before spending on it? Answering it needs the second half of `build-pass.sh`, which this pass could not fetch in one read. That is a bounded, mechanical question for a pass that can page a large file or grep the repo. NOT ACTING: no lane changed, no result recorded, no instrument set. Recorded as a new annotation rather than by editing the one above, so the mistaken claim and its withdrawal both stay visible.
- 2026-08-22 2026-08-22 unattended sweep, repo sight held: this ask is now substantially answerable from committed code, and the answer splits the question in a way the ask does not anticipate. Read this pass: `examples/automation/build-pass.sh`, `src/eval/buildable.ts` in full.

**Status IS re-read, so the literal question answers "live, not cached".** The script writes candidate titles to `$INSTRUMENTED` via `node "$CLI" buildable --vault .`, then puts every line back through `node "$CLI" buildable "$sol" --vault .` individually before believing it, with the comment "A title that does not survive its own permit check is a parse error, not a build candidate." Each of those invocations re-reads the vault. There is no cached file snapshot.

**But the permit it re-reads is a fact about the past, and nothing re-runs the instrument.** `buildPermit`/`permitFrom` is pure — it reads the recorded `## Instrument Log` lines and never executes anything. The function that re-runs the command is `confirmPermit`, and `buildable.ts` documents it as deliberately opt-in: "It is deliberately NOT folded into `buildPermit` ... the confirmation is opt-in and belongs to the caller about to spend something." No `confirm` invocation appears in the portion of `build-pass.sh` read this pass (start through the no-build-candidates report branch; the tail was truncated by the reader's cap, so this is a bounded observation rather than a proof of absence).

**The failure the ask was written about already has a dated first-party record**, in `buildable.ts`'s own docstring: "On 2026-08-06 the build loop spent a full model pass on 'A model reads the raw transcript and files what the pattern scan cannot see', whose instrument had been recorded red on 2026-08-05 with 'No test files found' and had been green since the merge 17 minutes earlier."

**Recommend narrowing this ask before putting it to anyone.** The useful remaining question is not "cached or live" — it is "does the build loop call `confirmPermit` before spending a model pass, and if not, why not". That is a smaller question and may be answerable by finishing the read of `build-pass.sh`'s tail rather than by occupying a person. No result recorded and no rung moved: reads of committed code, nothing executed.

## Pre-committed threshold

**Pre-committed threshold:** the answer names where the target list is built and states whether a status change written after list-build but before commit would be seen, and it answers separately whether the recorded red observation that mints the permit is revalidated before spend. Both halves are required — an answer covering only the first scores inconclusive, because this node conflates two questions and one of them is the one that costs money. Any "nothing goes stale here" claim rests on at least one file read in full, with truncation reported rather than glossed.

This paragraph adds no new question. It restates the bar the node's own first sentence already carries, in the bold pre-commitment form `askedOf` (`src/eval/coverage.ts`) reads — this node has no `threshold:` frontmatter field, so the prose scan classifies it, and a bare `Threshold:` line matches no lead-in and reads as `absent`.

The read-in-full clause is written from this node's own history: the 2026-08-22 annotation below claimed `confirmPermit` is invoked nowhere in `build-pass.sh`, and the correction beneath it withdrew that claim because the file came back truncated at 49,506 bytes. The bar is set so that answer shape cannot clear it next time.

What clearing this bar does not settle: whether the loop *should* confirm a recorded red before spending. That is a design decision among siblings and stays a human's.

_Added by the 2026-08-22 unattended sweep. No lane changed, no result recorded, no instrument set._
