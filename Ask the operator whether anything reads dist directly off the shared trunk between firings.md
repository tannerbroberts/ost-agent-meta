---
type: AssumptionTest
created: '2026-08-18'
evidence: assertion
lane: humans-required
threshold: >-
  Zero consumers found reading dist off the shared trunk between firings, or at
  least one named consumer
---
#AssumptionTest #unvalidated #evidence/assertion

Small, fast check: the operator (or someone with the deployment/install path open) confirms whether any consumer installs or loads dist/ straight off the shared trunk rather than from a per-firing branch or its own build step. Threshold: no such consumer, or at least one named.

A person outside the building is the measurement here: Which systems actually install or load from the shared trunk's dist/ is knowledge about deployment, not something this repository alone can answer — the operator or whoever set up the consuming systems has to say.

## Issues
- 2026-08-21 2026-08-21 (unattended sweep) — the repository names a consumer, so the "zero consumers" branch of this threshold is already closed and the remaining question is narrower than the node states. The node says "Which systems actually install or load from the shared trunk's dist/ is knowledge about deployment, not something this repository alone can answer." The repository answers half of it. `examples/automation/build-pass.sh` sets `CLI="$OST_AGENT_DIR/dist/ost-agent.mjs"` with `OST_AGENT_DIR` defaulting to the script's own checkout root (`$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)`), and then invokes `node "$CLI"` on every firing for `build-check`, `gate`, `buildable`, `verify`, `check` and `debt`. It runs the committed bundle directly and has no build step of its own — there is no `npm run bundle` anywhere on that path. So against the threshold as written ("Zero consumers found reading dist off the shared trunk between firings, or at least one named consumer"), AT LEAST ONE CONSUMER IS NAMED: the build loop itself, on every firing, for six separate CLI calls. WHAT IS STILL THE OPERATOR'S TO ANSWER, stated so this annotation is not read as more than it is: the script reads dist from whatever `$OST_AGENT_DIR` points at, and whether the operator's actual cron entry points that at the shared trunk or at a per-firing worktree is deployment configuration this repository does not contain. The residual question is therefore not "does anything read dist off the trunk" but "is OST_AGENT_DIR the trunk in your deployment" — a smaller and much cheaper thing to ask. WHY THIS MATTERS TO THE BRANCH ABOVE IT: this test sits under the dist-merge-conflict work, and the answer changes which solution wins. If the build loop is reading the trunk's dist between firings, then any candidate that stops committing dist, or that leaves it briefly unbuilt mid-merge, breaks the loop that is doing the building — which is an argument for the merge-driver candidate (dist stays committed, conflicts are resolved by rebuilding) over any candidate that untracks the artifact. NOT ACTING ON THIS: no lane changed and no instrument set; the label is humans-required and removing a caution is not this surface's call. A human who agrees the repository has closed the first branch can re-lane with: ost-agent lane "Ask the operator whether anything reads dist directly off the shared trunk between firings" --set compute-only. Source: this pass's `ost_read_repo` read of examples/automation/build-pass.sh and .gitignore. First-party read of committed code; grounds feasibility only. No command executed, no result recorded, rung unchanged.
