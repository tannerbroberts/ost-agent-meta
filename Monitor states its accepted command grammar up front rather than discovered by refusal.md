---
type: Solution
source: 'TRANSCRIPT:0f28d01f-35fa-49f0-b085-89170e306ef8'
created: '2026-08-18'
evidence: assertion
authorship: machine
---
#Solution #unvalidated #evidence/assertion
[[Publishing Monitor's accepted grammar up front would cut the refusal-then-retry cost these transcripts show]]

Surface Monitor's constraints (no command substitution, one operation per call, working-directory-scoped reads) in its own tool description or a preflight probe, so a composing agent can shape a compliant command on the first attempt instead of spending a turn on refusal and a retry to learn the boundary empirically.

## Issues
- 2026-08-29 2026-08-29 unattended sweep, repo sight held: examined for a missing instrument and deliberately left without one. Recording the examination because this node carried no prior note. Two separate reasons, and the second is the one that would otherwise get re-litigated. First, the artefact is Monitor, a harness tool: no spec under this repository's `test/` can assert anything about its tool description, so the mechanical half is out of reach here — the same blocker already recorded on the sibling "Adopt the platform's own job-control wait instead of maintaining a bespoke shell helper". Second, the belief beneath it, "Publishing Monitor's accepted grammar up front would cut the refusal-then-retry cost these transcripts show", is a counterfactual about how an agent would have composed differently given information it did not have; a spec cannot run the counterfactual, and the corresponding ask is already on the standing queue as "Ask the operator whether reading Monitor's stated constraints before composing would have changed how these two sessions wrote their commands". Worth recording alongside: this firing's own corrections preamble carries the Monitor `sleep` refusal and its permitted replacement, which is this candidate's mechanism already partly delivered by a different surface — evidence about the idea, not a reason to instrument the node. What a human should do: set the lane with `ost-agent lane --set`, since `ost_flag_humans_required` is withheld on the unattended surface. Not a skipped step.
