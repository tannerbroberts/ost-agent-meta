---
type: Assumption
source: 'agent-ideation:2026-08-21-unattended-sweep'
created: '2026-08-21'
evidence: assertion
---
#Assumption #unvalidated #evidence/assertion

**Kind: feasibility, with a usability edge.** The belief: what the four passes lacked was disclosure of the key, not the discipline to use it — so an issue string that names `ost_annotate` with its own exact text as the third disposition is both emittable by the scanner without breaking the existing whole-string match, and used by the next pass that meets a new flag. Stated so it could be false: if a pass given the instruction still writes its verdict in prose, the key was never the problem and the sibling solutions (looser matching, or a human-only ledger) are the right shape instead.

**What the mechanical half settles and what it leaves.** A spec can prove the string carries the instruction and that annotating with the full string still clears — feasibility. It cannot prove a pass will read and quote it; that is observed only across future firings, and a green spec says nothing about it.
