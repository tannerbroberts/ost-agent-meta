---
type: Opportunity
source: 'REPO:src/eval/buildable.ts'
created: '2026-08-30'
evidence: assertion
authorship: machine
---
#Opportunity #unvalidated #evidence/assertion
[[The bucket gains the lane filter the function two doors down already has]]
[[Route the humans-required solution into the ask queue instead of dropping it from the instrument queue]]
[[Exclude only on a lane a human set, copying the shipped-status audit rather than trusting the field]]

**The need.** When I decide that a question can only be answered by a person, I want that decision to be recorded as a decision — not to reappear on every future pass as an outstanding failure to write a command. Today the opposite happens: the deliberate answer and the un-done work are indistinguishable in the queue, so every firing pays to re-derive a conclusion the tree already reached, and each one arrives at a node whose prose is arguing with the counter that summoned it.

**Litmus test (more than one way to address this?):** Yes, and the candidates disagree about who should act. Filter the bucket in code so the listing never mentions them. Route them to `outstandingAsks`, which already exists and already ages a pending ask, so the work is re-labelled rather than hidden. Or copy `trustsShippedStatus` exactly and exclude only on a lane a human's `ost-agent lane --set` recorded in History, distrusting the bare frontmatter field. Passes.

**What this node does not claim.** It does not say every bucket entry is of one kind. It does not say `humans-required` is the correct lane for any particular one — several entries argue their own case and a reader may disagree. And it says nothing about desirability: this is observed behaviour of the product, read from its own source and its own sweep output, which grounds usability and is not evidence that anybody outside this project wants it fixed. Rung `assertion` — a claim from inside the building. No test has been run on it and no result is recorded.

## Standing finding: the mechanism, read off the source and confirmed twice by arithmetic

**The filter.** `solutionsMissingInstruments` in `src/eval/buildable.ts` (read in full via `ost_read_repo`) clears a Solution from the bucket on exactly three conditions: `status === "deferred"`, `trustsShippedStatus(n)`, and any test beneath it carrying a parseable instrument. **There is no lane check.** Two functions below it in the same file, `testsAwaitingVerification` does have one — `if (n.lane === CAUTIOUS_LANE) continue;` — with a comment explaining why: "A test a human put beyond compute's reach is never run by compute." `CAUTIOUS_LANE` is `humans-required` and `computeMayRun` returns false for it (`src/knowledge/lanes.ts`). So the codebase already holds the exact predicate that would settle this, applies it to one queue, and does not apply it to the other.

**The consequence, and why it compounds rather than merely annoys.** A Solution whose every assumption test is correctly labelled `humans-required` can never leave this bucket. The only call that could label a test into that lane from an unattended surface is `ost_flag_humans_required`, and it is not granted on `/ost-pass` — pinned in `test/eval/clearability.test.ts`, whose `lane-conflict` row records the reason as "on `/ost-pass` the tool is not granted at all (R7's containment)". So the sweep is handed a bucket, told to clear it, and holds neither the tool that would clear it nor an honest command to write instead. The instruction and the grant disagree, and the pass in the middle absorbs the difference.

**The precedent is in the same file.** `solutionsMissingInstruments`' own doc comment records that "Four solutions correctly parked as `shipped` recurred in this list on every pass until each future sweep either re-derived the same conclusion or was tempted to invent a command that could not fail." That is this defect in different clothing, already recognised and already fixed once — for `shipped`, by `trustsShippedStatus`. The humans-required case is the same failure with the same two outcomes, and the second is the dangerous one: a pass that resolves the pressure by inventing a command puts a test into the tree that cannot measure the belief it hangs under.

**Confirmed twice by counter arithmetic, independently of reading the source.** Two firings each created three Solutions in one sitting — two whose tests carry an `instrument:`, one whose test was created with `humansRequired:` — and re-ran the sweep. On 2026-08-30 the bucket moved **68 → 69**; on 2026-09-08 it moved **74 → 75**. Both times exactly the humans-required candidate entered and both instrumented siblings stayed out. The instrumented pair is the control — same author, same pass, same parent — so the difference is the lane and not the writing or the age of the node. The second run matters especially because that pass's own `ost_read_repo` read of `src/eval/buildable.ts` **truncated at the `solutionsMissingInstruments` docstring**, so the source route was unavailable and the arithmetic route reached the same answer without it.

**What the control rules out.** That the bucket is simply full of old nodes written before lanes mattered. A node created under the current rules, with a lane set deliberately at creation, lands in the bucket immediately.

**The trap, and it decides between the three candidates.** A lane reaches a node two ways: a human's CLI call, or `ost_create_node`'s own `humansRequired:` argument, which an agent passes at creation time. A filter that trusts the bare field lets an agent take its own work off the queue by declaring it needs a person — the same self-certification `trustsShippedStatus` was written to refuse. Whichever candidate is chosen has to answer that, and any answer that trusts the bare field is choosing to.

## Standing census: the stock, its kinds, the flow, and the residue

Five stock counts were taken across passes at widening scope. Where they disagree the widest supersedes; the trajectory is kept because it is itself a finding.

**The kinds — five, from a 10-of-25 head census (2026-08-30) plus a sixth-strata pass (2026-08-31).** Ten of ten entries opened were legitimately not-instrumentable; not one was a pass that simply failed to write a command.

- **A humans-required belief.** "An operator-set evidence window in ost.config.yaml, amended by hand like discovery.target", "Group the queue by error signature at read time, and change nothing on disk", "A human-edited manifest of loop-prescribed call sequences the harvester suppresses", "Name the specific mechanisms a hand process structurally cannot have", "Axioms elicited at the moment a derivation needs them, one accept-or-reject ask at a time", "Route the humans-required solution into the ask queue instead of dropping it from the instrument queue". Each rests on whether a person answers, maintains, acts, or is persuaded.
- **The subject is not in this repository.** "A repeated wait on the same condition resumes and doubles its budget automatically, up to a ceiling set once by hand" is about the harness's `await` helper on the session PATH; "Maintain a running per-item task list the next pass reads before reconstructing state itself" is about the harness's own task list; "Auto-read a file before the first write or edit to it in a session, instead of erroring" is about Claude Code's `Write`/`Edit` precondition. No spec under `test/` can reach any of them, however the belief is worded — and none should be labelled humans-required, because the question is about code, just not code that lives here, and filing it in the interview queue would leave it waiting forever.
- **Already built; a spec would pass on arrival.** "Remote push optional and off by default" is pinned green in two places (`test/config/load.test.ts` asserts `cfg.remote.enabled` is false; `test/release/outward-mutation.test.ts` withholds `git_push` from the agent surface with a positive control).
- **The premise is superseded and needs a human's disposition.** "Append-only tool surface with no delete or shell tool" describes a surface that no longer exists. A spec asserting "no delete tool" would go red — and that red would invite a builder to *remove* `ost_merge_nodes`, which is worse than no spec.
- **The market kind, which has no artefact inside the building at all.** "Concierge design-partner cohort", "Charge for the maintained tree, not the tool that maintains it", "Charge nothing for the tool and sell the discovery engagement it produces", "Instrumented public trial with a willingness-to-pay probe", "Continuous story-based interview habit". It is not that a spec would be dishonest; there is nothing for a spec to point at.

**The selection effect that hid the fifth kind, worth keeping as method.** All five market-kind entries carried no examination note of any kind until 2026-08-31, while nearly every other head entry carried one. That was not coincidence: the earlier censuses state their own sampling rule — the head was worked "by which titles looked most likely to be repo-answerable", "deliberately biased *towards* finding instrumentable entries". A rule that opens the plausibly-instrumentable first leaves exactly the never-instrumentable unopened, so the stratum with the cheapest and most certain answer is the one that got re-derived every pass. All five are also the oldest stratum in the bucket — `source: agent:P3_ideate` or no source at all, History lines dated 2026-07-24 and 2026-08-05 — sitting roughly five weeks without one being opened.

**The whole-tree stock, latest count (2026-09-08).** **512** files carry `type: AssumptionTest`; **377** carry an `instrument:` field *and* `type: AssumptionTest` within the frontmatter window; **66** carry a `lane:` field, all 66 AssumptionTests. Overlap verified first-party rather than assumed: a multiline search for any file carrying both `lane:` and `instrument:` in either order returns **zero**, so the three sets partition and **69 tests carry neither**.

**The trajectory, and the withdrawal it forces.** Corpus 491 → 500 → 512; instrumented 358 → 371 → 377; laned 60 → 62 → 66; carrying neither 73 → 67 → **69**. A 2026-09-01 section read the two-day movement as favourable — "the debt is draining, slowly, and not growing." Re-taken seven days later by the identical method it does not hold: twelve new tests, six newly instrumented, four newly laned, and the population that is neither runnable nor assigned grew by two. **A reader weighing the three candidates should not carry the draining premise forward.**

**What the unlabelled population actually is — the classification of all 67, item by item (2026-09-01), which bounds the fourth candidate.**

- **49 (73%) — a person is irreducibly the measurement.** Interviews and market probes ("Ask ten buyers what happened the last time a tool they relied on shut down", "Interview ten solo builders about the last decision they made with nobody to check it", "Pre-order probe - will anyone pay before the map proves itself", "Offer a maintained tree at a stated monthly price to ten teams and count who asks for terms"); operator and reader studies ("Show the operator ten forks already taken and count the reversals and the minutes", "Show readers a degraded run report and see whether they notice", "Five-second status glance test after a failed run"); and hand-grading arms ("Hand-distil three past sessions", "Diff three past sessions' claims against their traces by hand", "Have a second person run the hand arm, so the comparison is not built by its own author"). No exit code reaches any of them.
- **6 (9%) — longitudinal, waiting on elapsed time rather than on anyone's judgement.** "Set up one scheduled export and check every week whether it is still arriving", "Follow a candidate source list for a month and count items that bear on anything open", "Settle the known prompts as config and count how many new ones appear in a month", "Do named unfixed thresholds actually get fixed", "Two unattended weeks - count pages, grind, and money burned", "Does a placeholder outcome get replaced, or does it become the tree's real root". Neither humans-required nor instrumentable today — questions with a clock on them, and the queue has no shape for that.
- **12 (18%) — plausibly settleable by a spec or a replay, and therefore genuine instrument debt.** "Replay ten past runs and count how many needed a scope nobody would have predicted", "Count how many of the operator's real experiment sources can push anywhere at all", "Test humans can promote while the agent is blocked from validating", "Check what would actually have to be redacted before a vault could live in a shared repo", "Do the shipped sweeps actually find a planted instance", "Test can a full pass be done with no delete or edit tool", "Sweep both vault histories for writes that landed as undefined or empty", "Rewrite the shortest helper against the bundled runtime and compare length and readability", "Backdated half-life comparison for staleness flags", "Does refusing a newline inside a wiki-link catch breaks nothing else catches", "Count how much post-handoff work in past sessions would have survived a failing check", and "Apply the escalating message to the five-failure session and check where it would have fired" — the last already `deferred` and retired, so the live figure is **11**.

**What that does to the fourth candidate.** A blanket "label the unlabelled" is **correct for 49, wrong for 12, and ambiguous for 6** — so its honest yield is about 49, and it needs a triage step in front of it rather than a bare grant. Symmetrically, the residue that is *actually* what the ruleset calls debt is **11 live tests**, not 67 — an order of magnitude smaller than the bucket it feeds, and small enough for one attended pass to work through by hand. One of the 12 is stale rather than owed: "Test can a full pass be done with no delete or edit tool" measures a premise the product abandoned, and its parent solution already carries a standing (a)/(b) ask to a human on exactly that.

**Why no pass has set an instrument on the 11, and why that is a property of the surface rather than reluctance.** An instrument must be red for a reason specific to the question it answers. An unattended surface can neither write a spec file nor run one, so every command it could compose would name a spec that does not exist, fail identically for any question written on it, be filed `no-spec`, and grant no permit — the defect the ruleset records as 260 of 266 recorded reds reading "No test files found". There is a second, stronger reason recorded elsewhere on this tree: the accepted instrument grammar names a whole spec **file** with no test-name filter, so against a suite kept green a named spec either passes today (refused as already-built) or does not exist (`no-spec`). Composed, the two say the residue is not merely unworked on this surface — it is **unworkable** on it, and the pass that reduces the 11 will be one that can write a failing spec.

**The flow, which no stock count sees, and it is the finding that re-ranks the candidates.** The bucket stood at 68 on 2026-08-31 and 74 on 2026-09-08: **+6 in eight days, ≈0.75/day**. And the new entries are not un-examined work — four head entries opened on 2026-09-08 ("Hand the oversized body to the harness's own compaction and store nothing new", ideated 2026-08-31; "Name the engine's published dialect in the tool description and build no validator of our own", 2026-09-03; "Group the queue by error signature at read time, and change nothing on disk", 2026-08-30; "A dead-man's-switch outside the host, because an in-host watchdog dies with the host", ideated and annotated the same day) **all four declare their own refusal in their own body**, under a `## Definition of done — and it is not a command` heading written by the pass that created them. So the re-derivation cost this node was created to name has largely been paid down: conclusions are now written at creation time. What remains is simpler and worse — **the bucket's inflow is the tree's own humans-required ideation rate, so it cannot reach zero, and it holds `done: false` on this vault indefinitely.** A filter is worth building in proportion to the flow it stops, not the stock it clears; if the count is still climbing at ≈0.75/day when someone looks, the mechanism holds, and if it has flattened, the ideation rate changed and not the queue.

**How far each candidate actually reaches.** The **lane filter** clears only the labelled subset — at most the 66 frontmatter labels, and only if each is the sole test beneath a distinct bucket solution, an upper bound rather than an estimate. The **routing candidate** inherits the same ceiling: it can only route what carries a label. The **lane audit** is bounded by a subset of the 66 — those whose lane a human's `ost-agent lane --set` recorded in History rather than an agent's `humansRequired:` at creation — and both lanes read directly by the head census were agent-set ("Count the recorded expiries that later succeeded against those whose condition could never have become true" carries `lane: humans-required` set at creation; "Test do operators get value with remote push off" carries `lane: null` and is pure desirability, because the sweep that met it had no tool that could label it). So the audit is the candidate that best answers the self-certification trap and, on today's data, also the one that does least. And **every one of the three leaves the subject-outside-the-repo kind exactly where it is**, because all three key on the lane. Whoever builds any of them should expect the bucket to shrink and not to empty, and the residue will be that kind. A disposition for "no spec in this repo can reach this", parallel to the lane, is one shape for it; whether that is a fourth solution here or a need of its own is a human's call and is not proposed here.

**Limits over the whole census.** The stock counts are file-level frontmatter over the vault's Markdown, not a walk of the tree, so nothing maps tests onto bucket entries — several may share a parent, and the mapping from labels to entries is bounded above and not computed. Two files match `^instrument:` without matching `type: AssumptionTest` inside a 600-character window and are excluded, the same caveat an earlier count records at six files, so 69 is an upper bound in the same direction. The 12/6/49 classification was made from each test's title plus eight bodies read in full, not all 67; titles in this vault are unusually declarative, which is why that was judged affordable, but the boundary most likely to move is between the longitudinal six and the human 49, since a wait can usually be handed to a person. The head censuses cover the 25 `ost_next_work` shows, which is the unfiltered head ordered by id and not a random sample. Two measurements nine days apart are an interval, not a trend. A count of labels is not a judgement about whether any label is correct. Nothing above was executed, no rung moved, no instrument set, no status changed.

_Method: read-only `Grep` over `*.md` in this vault matching `^type: AssumptionTest`, `^instrument:` and `^lane:`, plus a multiline overlap check; `ost_read_tree` reads of solution, assumption and test bodies; `ost_read_repo` reads of `src/eval/buildable.ts`, `src/knowledge/lanes.ts`, `src/ost/lanes.ts`, `src/eval/coverage.ts`, `src/ost/legacy-fallback.ts` and several spec files; and successive firings' own `ost_next_work` responses. Observed structure of this product and its vault; grounds usability and feasibility, never desirability._

## The stopping condition: read the notes, do not sample again

Every one of the 25 entries `ost_next_work` shows now carries a recorded examination with a stated reason for declining, and new arrivals come pre-annotated. So the head is exhausted: there is no entry left in view for a future pass to examine, and examining is the only honest act an unattended surface has here. **A firing that meets this bucket should read these notes and stop, not sample again.** The value of the earlier samples was that the population was uncertain; for the visible part it no longer is, and a pass that re-derives it is paying exactly the cost this node exists to name.

It has been confirmed and re-confirmed rather than tested: a 2026-09-07 firing opened four head entries and found four of four already carrying written reasoning; a 2026-09-08 firing did the same and found the same; a 2026-09-08 firing sampling six found three kinds, all already recorded here. Each of those samples is itself the cost this section exists to stop.

**The one route past the cap, and its price.** The cap is a queue, not a wall: every honest drain advances it by exactly one. "Remote push optional and off by default" was set `shipped` after both its pinning specs were read in full, the bucket went 69 → 68, and one previously-invisible entry rose into the shown 25 — "Run the same evidence through both and publish the diff as the pitch", which turned out to be humans-required and carrying no lane, i.e. the same shape as the head. That is n=1 from the tail, drawn by whichever entry sat at position 26 in id order, so it is not a sample of the tail; it is one data point against the hope that the unreachable entries are where the instrumentable work has been hiding. A future pass wanting to read further now knows the price — one honest disposition per entry revealed — and knows that dishonest dispositions buy the same visibility, which is the hazard named below.

## The one exclusion an agent can write itself, and pulling it would be a lie

Of the three conditions that clear a Solution from this bucket, exactly one is writable from an unattended surface, and it is the bare one. `trustsShippedStatus` was built specifically to refuse self-certification — its doc comment says the exclusion "does not trust the bare field — `status: shipped` is agent-settable — only a promotion recorded in `## History` with reasoning attached leaves the queue." The instrument condition needs a command that fails today. But `deferred` is excluded on the bare field, deliberately and with a stated rationale: "unlike `shipped`, `deferred` makes no claim about code that exists to audit against the repository." And `ost_set_status` offers `deferred` on the unattended surface, ungated.

**So the bucket a pass is told to clear, and cannot clear honestly, has precisely one lever the pass can pull unilaterally — and pulling it is a lie.** `deferred` means abandoned. Every entry every census here has classified is the opposite: waiting on a person, or outside `product.repos`, or already built. A firing under standing instruction to drain the bucket, holding no `ost_flag_humans_required`, could empty it completely with one status call per entry and produce a green sweep in which nothing was decided and every live candidate was retired. Nothing in the code, the ruleset, or this node currently says not to.

The asymmetry is the finding: the codebase distrusts the agent on `shipped`, distrusts it on the instrument, and trusts it on the one status that silently retires a candidate. This node's own trap paragraph says "any answer that trusts the bare field is choosing to" and applies it to the lane; the reasoning transfers exactly to `deferred`, and there it is not a design question for a future candidate — it is shipped behaviour with an agent-writable key. None of the three candidates addresses it, because all three are about adding an exclusion and this is an exclusion that is too easy to satisfy. Whoever builds one should decide separately whether `deferred` deserves the `trustsShippedStatus` treatment, or whether the honest answer is that a bucket with no honest exit should not be a bucket a pass is instructed to clear.

**What the passes that found this lever did with it: nothing.** No status has been changed on any bucket entry by any pass that recorded this. Stated explicitly so a later reader can tell the hole was found and left alone rather than found and used — and because this vault carries a category named "The agent narrows its own capability to get past a gate I set", of which this is an instance with the narrowing available for free.

## What the bucket cannot say at all: the half-instrumented solution

Every census above studies solutions wrongly *in* the bucket. This is the opposite direction and it is not a restatement of the lane argument.

`resolveTestsUnderSolution` walks Solution → *every* Assumption → *every* AssumptionTest beneath each and returns the flat union; `solutionsMissingInstruments` then clears a Solution on `tests.some((t) => nodeInstrument(t.test))`. So **one** instrumented test anywhere under a solution removes it, however many of its other assumptions carry no runnable test at all. A solution resting on four beliefs with one instrumented test is reported as served, identically to one whose four beliefs are all instrumented.

Read against that function's own stated purpose — solutions that "cannot reach a builder, because everything written about it is prose" — `.some()` is the right predicate: one red command is enough for work to start. The gap is that no other surface picks the question up. `src/eval/coverage.ts`, read in full, classifies thresholds and pairs recorded results against `## Uncovered` statements; it never asks whether each assumption beneath a solution has a runnable test. `solutionsMissingAssumptions` counts solutions with no test at all. **Nothing counts the middle.**

Where that becomes a claim rather than an omission: `permitFrom` selects `live[0]` — the first red test it finds — and returns the reason string "`<command>` fails today and passes when `<solution>` is built. **That is the definition of done.**" On a solution resting on four assumptions with one instrumented test that sentence is false in the direction that costs most: a builder turns one command green and the permit reads as spent, while three beliefs were never given a runnable form. This is the failure the Assumption layer was introduced to prevent — `ost_create_node`'s own description says "a solution resting on four beliefs is not covered by one test against one of them — which is the distinction this layer exists to keep." The layer keeps the distinction; the permit that reads the layer discards it.

**The size of the masked population is not established.** The instrument-less tests cannot be mapped onto bucket entries without a per-solution join, and that join is not computable from read-only greps. What the mechanism does establish is that masking occurs whenever an instrument-less test shares a solution with an instrumented one, and that no queue in the codebase would report it if every such test in the vault were of that kind. None of the three candidates would surface it either, because all three operate on entries the bucket already lists. Whether a solution's assumptions should be counted individually, and whether a build permit should say how many of them its one command covers, is a fourth question and a human's.

## The fourth candidate already ships, and it misses this vault

**The prose-declared population.** `proseDeclaredLane(node)` in `src/ost/lanes.ts` returns the lane a test declares in its own prose but not in its frontmatter, and `triageLanes` already surfaces those as `proseDeclared` alongside `laneConflicts` and `proseLaneAmbiguity`. It is not a naive regex, and `test/ost/lanes.test.ts` says so with cases taken from this vault: it ignores the `lane: X → Y` audit line `setLane` writes under `## History`, ignores a lane named in an `## Issues` annotation, refuses a split declaration such as "Lane: compute-only for the census, humans-required for the fixing" by returning it as an ambiguity rather than taking the first match, and drops an unrecognised lane name. It reads AssumptionTests only. Counted whole-tree on 2026-08-31: 494 files carry `type: AssumptionTest`, **127** carry a prose declaration matching `^**Lane: ` (all 127 AssumptionTests, checked by requiring both in one multiline match), 60 carried a frontmatter `lane:`, and exactly **14** carry both — so **113 AssumptionTests declare a lane in prose that their frontmatter does not carry**, and prose declarations outnumber frontmatter labels 127 to 60. A candidate reading the declaration rather than the label therefore has 113 more nodes in reach. But a prose declaration is agent-written, exactly like `humansRequired:`, so a filter trusting it lets an agent take its own work off the queue by writing a sentence: it buys **reach, not authority**, and sits on the same side of the self-certification line as the lane filter.

**The bulk labeller is not a build and not a grant — it already exists.** `test/cli/lanes.test.ts`, read in full, pins **`ost-agent lanes --flag-cautious <by>`**, and it does the fourth candidate's job today with exactly the safety properties this node has been arguing one would need. It labels in bulk (`flagged "Interview five real players"`, writing `lane: humans-required` into frontmatter). **It only ever moves toward a person** — the hint spec asserts `not.toMatch(/⚠ likely compute-only/)`, there is no permissive bulk call, and the comment says why: "the permissive call is a judgement and bulk is the wrong shape for it." **It never reverses a human's permissive call**, pinned by a dedicated test commented as "the one bulk failure that would be a safety regression rather than noise." And it refuses to read as finished: `1 remain unclassified` and `still NOT runnable by compute` are asserted, and an empty run prints `Nothing to flag` and never "safe to automate". The plain `ost-agent lanes` prints `unclassified N`, `Runnable right now (compute-only, no result yet): N`, per-test `⚠ likely humans-required` hints, and with `--runnable` a bare scriptable list; **nothing in the spec asserts a `proseDeclared` section**, so the 113 are not something a shipped command is pinned to print — though that is an absence from a spec rather than from the code, and is the weaker half.

So two framings this node used to carry were wrong in the same direction. It is neither "a permission decision, the operator's alone" nor "a build of one predicate": it is a command that already ships. The operator's cheapest move is to type `ost-agent lanes --flag-cautious <their name>` and read what it flagged.

**The residual trap on that recommendation.** `--by` is a free string — one test in that very spec invokes `cli(["lanes", "--vault", dir, "--flag-cautious", "loop"])`, so the by-value can be any text, including a loop's name. "Human-set" is only as true as that string. The command's authority comes from a person choosing to run it, not from anything the record can prove afterwards, and whoever builds the audit candidate has to decide what it does with a `--by` it cannot verify.

**And the yield problem, which qualifies the recommendation rather than withdrawing it.** `cautionBacklog` skips any test already carrying a lane and asks `suggestCaution` about the rest; `suggestCaution` matches `title + body` against `PEOPLE_MARKERS`, a deliberately narrow literal list — interviews, interviewing, recruit, strangers, participants, cohort, survey, pre-order, willingness to pay, consent, usability, design partners, `real (players|users|operators|teams|customers)`, `outside (teams|operators|people)`. Every one names a person **outside** the building. This tree's humans-required population is overwhelmingly about people *inside* it — the operator, the founder, "someone with the build loop's source open". Over the 48 distinct test titles an unattended surface can see (25 `assumptionWork.needsHumans` + 25 `outstandingAsks`, two shared), **zero carry any marker**: "Ask the operator whether…" does not match because the marker requires "real operators" or "outside operators", and "A human re-judges…" and "A person reads ten compacted bodies…" match nothing at all. The heuristic is not weak here; it is aimed at a different population than this vault accumulated.

**The near-miss makes it concrete and it is one word wide.** Three humans-required tests read in full — "A person reads ten compacted bodies and marks every claim the full body made that the rendering dropped", "Ask the operator whether they'd rather a warning let the write through than have it hard-blocked", and "Ask the founder whether a machine-cleared permit may start a build with nobody watching" — come from different sources and dates and all three end with the identical server-written sentence `humansRequired:` emits: **"A person outside the building is the measurement here: …"**. The marker is `/\boutside (?:teams?|operators?|people)\b/i`. "outside the **building**" does not match it. **The one sentence this product writes onto a test to mark it humans-required is invisible to the product's own humans-required detector.** The cheap repair is a marker for that boilerplate, which would catch every future `humansRequired:` test by construction. Run the command anyway — it is free, one-way, and cannot reverse a permissive call — but expect a low yield, and do not read `Nothing to flag` as "the backlog is classified".

**Limits on this section.** Read off specs and source, never from a run: `test/cli/lanes.test.ts` and `test/ost/lanes.test.ts` in full, `src/ost/lanes.ts` in full. The command is reported as *specced* to behave this way; nothing was executed. The zero-of-48 count is over **titles only** while `suggestCaution` reads the body too, so it is a lower bound on the marker's reach and not a prediction of output. All three tests quoted for the boilerplate already carry `lane: humans-required`, so `cautionBacklog` would skip them regardless — they evidence the near-miss, not the yield. The `^**Lane: ` pattern misses declarations written without bold markers or not at line start, which `proseDeclaredLane` would still read, so 113 is a floor. No claim is made that any of the 113 declarations, or any existing label, is correct.

## The counter-reading: this may be design rather than gap, and that question is prior to all three candidates

This node argues throughout that the missing lane check is a gap rather than a design choice, on the grounds that the precedent sits in the same file. One artefact cuts against that framing and should be weighed before any candidate is picked.

`test/mcp/lane-consumer.test.ts`, read in full, builds a fixture Outcome → Opportunity → Solution → one AssumptionTest and instruments that test deliberately, with a comment stating why: "Instrumented so the only thing this fixture can be not-done FOR is the lane question under test. **A prose-only test blocks `done` on its own term now**, which would make 'assumptionWork never blocks done' pass for the wrong reason." So the spec author knew a prose-only test blocks `done` independently of its lane, described it as current intended behaviour, and worked around it in the fixture by giving a test an instrument — a test the same file elsewhere routes through the humans-required lane. The instruments bucket ignoring the lane is therefore not obviously an oversight nobody noticed; at least one author met the interaction head-on and treated it as the way things work.

All three candidates propose adding an exclusion, which is the right shape *if* this is a gap. If it is deliberate, the question a human owes is prior to all three — whether prose-only-blocks-done is the design that should stand — and picking a candidate answers a question that was never asked. This does not settle which reading is right: a fixture comment is one author's working understanding, not a design record, and nothing here shows anyone weighed the humans-required case specifically. The spec file was read, not the implementation, and nothing was executed.

## Issues

- **2026-09-08 — this node's consolidation blocker is discharged, and the mechanism it recorded is false as stated.** Its 2026-09-07 note concluded that `ost_read_tree` serves this node truncated (50,000 of 79,217 characters), that `ost_edit_node` refuses a rewrite which cannot account for every stored heading, that "the headings inside those unread characters cannot be enumerated from this surface", and therefore that "no honest consolidation is composable here" and the node "can only grow" until a human repairs it on the CLI. The premise is wrong in one specific way worth recording, because it generalises: **the truncation is a property of `ost_read_tree`, not of the vault.** This vault's nodes are ordinary Markdown files in the working directory, and the harness's own `Read` and `Grep` reach them whole with no MCP cap and no additional grant — the full 304-line file and its complete 15-heading inventory were read that way this pass, which is what made this consolidation composable. The general form, for whoever meets the same wall on another node: when an MCP read truncates a vault node, read the file. That route was available on every pass that recorded this blocker, and none of them tried it.
- **2026-09-08 — a procedural correction kept because the lesson is cheap and the mistake is easy.** An earlier 2026-09-08 pass annotated this node claiming the subject-outside-the-repo kind was "a second population nothing on this tree maps". It was already mapped here and had been since 2026-08-31. The cause was procedural: that pass read this node through a persisted tool result, searched it for the mechanism it had come for, and wrote before reading the later sections, so a taxonomy a few paragraphs further down went unread. On a node this size that is the cheap mistake, and re-reading before annotating is what avoids it. What stood from that note — the 74 → 75 counter arithmetic — is folded into the standing finding above.
- **2026-09-08 — one errand is retired rather than reissued.** Two earlier sections left open whether any CLI entry point prints `proseDeclared`, and directed whoever holds a shell to run `grep -rn "triageLanes" src/cli/` or `ost-agent lanes --help`, noting that `src/cli/index.ts` is 177,250 bytes and cannot be served whole by `ost_read_repo`, that `src/cli/` holds only `index.ts`, `loop.ts` and `vault-option.ts`, and that `docs/reference/` holds no CLI reference. The question was substantially answered from `test/cli/lanes.test.ts` and the answer is in the fourth-candidate section above: the command exists, and no `proseDeclared` section is asserted in its output. The remaining uncertainty — whether the field is computed and unprinted, or printed and unasserted — is an absence from a spec rather than from the code, and it does not change any recommendation this node carries. Future passes should not re-spend on it.

## History
- 2026-09-08 body edited, dropping `## The defect demonstrated itself within one pass, with a control`, `## The census, run over 10 of the 25 visible entries — and it is not a backlog (2026-08-30)`, `## The tail this node called unreachable, counted over all 491 tests (2026-08-30)`, `## The one exclusion the agent can already write itself (2026-08-30, third firing)`, `## The cap moved, and the first tail entry ever seen is the same shape (2026-08-30, fourth firing)`, `## The bucket's other direction: nothing anywhere reports a partly-instrumented solution (2026-08-31)`, `## A fifth kind, and the sampling rule that guaranteed it stayed unexamined (2026-08-31)`, `## The head is now fully examined, which is a stopping condition rather than another census (2026-08-31)`, `## The fourth candidate may need no build at all, and nobody has checked (2026-08-31)`, `## The errand left open above is closed, and the fourth candidate is already shipped (2026-08-31)`, `## The shipped fourth candidate has a yield problem on this vault specifically (2026-08-31, later firing)`, `## 2026-09-01 — the unlabelled population classified by what could answer it, which bounds the fourth candidate for the first time`, `## 2026-09-08 — the bucket has a measured inflow rate, and the entries arrive pre-examined`, `## 2026-09-08 (later firing) — the residue is not draining, and the 2026-09-01 claim that it was does not survive a seven-day re-measure` — Fourteen dated census sections had accreted here, growing this Opportunity to 79,217 prose characters — the exact shape the rollup replaced, on a node whose own 2026-09-07 note says so. Two of its notes ask a human to consolidate it and record that no pass can: `ost_read_tree` truncates at 50,000 characters, `ost_edit_node` refuses a rewrite that cannot account for every stored heading, and the hidden headings "cannot be enumerated from this surface". That premise is false and this write is the demonstration — the vault's own Markdown is directly readable with the harness's file tools, so all 15 headings were enumerated and every section read in full before composing. Consolidating exactly as this node's own 2026-09-07 note prescribes: the four stock censuses supersede each other on the same numbers (4-of-68, 10-of-25, 491/358/133/60/73, 500/371/62/67, 512/377/66/69), so only the widest of each survives, and the 2026-09-08 flow measurement survives as the one rate. No distinct claim, number, source citation, mechanism or limit is dropped — every one is reproduced below, superseded figures kept as trajectory rather than deleted, and git holds the full prior text. This is the same repair a 2026-08-21 pass applied to "Append-only tool surface with no delete or shell tool" and a 2026-09-03 pass applied to "A human-edited manifest of loop-prescribed call sequences the harvester suppresses", for the same reason: a node whose ask is buried under fourteen restatements is a node the human it is addressed to will not read to the end of.
