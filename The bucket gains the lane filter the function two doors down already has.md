---
type: Solution
source: 'agent-ideation:2026-08-30-unattended-sweep'
created: '2026-08-30'
evidence: assertion
killIf: >-
  A firing still opens a humans-required solution to re-derive why no instrument
  belongs on it, in a pass run after the filter shipped
killBy: '2026-11-30'
authorship: machine
---
#Solution #unvalidated #evidence/assertion
[[A solution whose every test compute may not run holds nothing a builder could start on today]]

**Variation dimension: who-does-the-work. Position taken: nobody — the step is deleted, not reassigned.**

Add one line to `solutionsMissingInstruments` in `src/eval/buildable.ts`: a Solution whose every resolved test sits in a lane `computeMayRun` refuses is not listed. That is the predicate `testsAwaitingVerification` already applies in the same file, so this borrows a rule rather than inventing one, and the two queues stop disagreeing about whether a lane label means anything.

**Why this position and not another.** The sibling candidates both keep the item in front of somebody — one moves it to the ask queue, one makes the exclusion conditional on who set the lane. This one says the listing was simply wrong: the bucket's stated purpose is solutions that "cannot reach a builder, because everything written about it is prose," and a test in the humans-required lane is not prose that failed to become a command. It is a finished decision about who the measurement belongs to. Nothing needs routing, ageing or re-deciding, so the cheapest correct answer is for the queue to stop saying it.

**Cheapest form.** One `continue` guard beside the two already there, importing `computeMayRun` from `src/knowledge/lanes.ts`, plus a row in the doc comment recording why — in the same voice the `shipped` exclusion is already recorded in, since that paragraph exists precisely because the same defect went unexplained once before. *Amended 2026-09-11:* it is one line in each of **two** functions, plus a refusal string that has to be rewritten rather than guarded. The standing finding below has the site list and what it costs this candidate.

**What it deliberately does not do.** It builds no ask queue, no ageing, no report of what it removed. A reader of the sweep will not learn that these solutions exist at all from this bucket.

**What it gives up, plainly, and this is the sharp cost.** Silence is indistinguishable from correctness. Once excluded, a solution whose lane was set too cautiously — by an agent passing `humansRequired:` at creation because it could not think of a command, which is a real and cheap failure mode — disappears from the one list that would have surfaced it, permanently and with no trace. The sibling that distrusts the bare field is built entirely around that risk; this candidate accepts it in exchange for costing one line. It also does nothing about the tests already waiting on a person: it makes the queue honest, not shorter in any way that gets work done.

**What would make this the wrong pick.** If a meaningful share of the bucket carry `humans-required` because a pass defaulted there rather than because a person judged it, this filter cements those defaults out of sight and the tree quietly loses tests it should have argued about. That is the disqualifying condition, it has now been measured, and the census below reports it at **0 hits across 25 of 25 traced entries**.

**Honest note on how this was ideated.** The sweep asks for one blind ideator per dimension. This surface holds no grant to run independent parallel ideators, so all three candidates under this opportunity were composed in one context by one author — the exact condition the blind-ideation rule exists to prevent. Read them as one author's three answers and discount their apparent distinctness accordingly.

Unvalidated. Agent-ideated 2026-08-30; a human to review.

## Definition of done

"An all-cautious solution leaves the instrument bucket while one with a runnable test beside it stays"

```
npx vitest run test/eval/lane-aware-instrument-bucket.test.ts
```

The bar, pre-committed: 0 solutions carrying a compute-runnable test are dropped, and at least 1 all-cautious solution is. The spec does not exist yet, so this command is red for the weak reason — it names a missing file rather than a failing assertion, and an unattended sweep holds no write grant on the product repository to fix that. The threshold is what a builder acts on until the spec is written.

The test title is quoted rather than wikilinked on purpose: its one backlink belongs to its parent assumption.

## Standing finding: the fix is two sites, not one

Established 2026-09-11, read first-party. Every census on this node from 2026-08-30 onward located the defect in exactly one function, quoting the original finding that `solutionsMissingInstruments` "applies no lane check at all, while `testsAwaitingVerification` two functions below it in the same file does." That is true and it is not the whole site list.

**The second site.** `permitFrom` — the function `buildPermit` and `buildableSolutions` both route through — filters a solution's tests with `tests.filter((t) => nodeInstrument(t.test))` and applies no lane predicate. When that set is empty it returns a refusal whose text is: "none of the N test(s) under X declares a runnable instrument, so none of them can go red or green. Add an `instrument:` naming one spec file to: ..." followed by every test title, laned or not.

**Why that is worse than a second copy of the same omission.** The refusal does not merely fail to exclude a humans-required solution — it instructs the reader to do the one thing the lane exists to forbid, and names the specific tests to do it to. By this file's own account that instruction cannot be carried out: the comment inside `testsAwaitingVerification`, eight lines below, states that `ost_set_instrument` "refuses to create that combination" and calls its own lane check "the second lock on the same door." So `ost-agent buildable` on an all-cautious solution today prints a next step the write surface would reject — worse than a queue listing something it should not, because a silent over-count sends a pass to re-derive a conclusion while this sends it to attempt a refused call.

**What it costs this candidate, and it is a genuine cost.** The sharpest thing this node has going for it is cheapness. That is now one line in each of two functions, plus a refusal string rewritten rather than guarded — because the honest message for an all-cautious solution is not "add an instrument" with a shorter list, it is that the solution's definition of done belongs to a person and no permit is coming. A rewritten refusal is prose someone has to compose, which is exactly the work this candidate's "nobody — the step is deleted" position claims to avoid. It does not sink the candidate; the siblings inherit the same second site, and one of them (routing to the ask queue) arguably has the better answer for the refusal text, since it already has somewhere to send the reader.

**The cheap check this hands a builder.** Both sites are in one file and both want the same import, so this is still a small change — but a spec asserting only the bucket's behaviour will pass while `ost-agent buildable` keeps printing the bad instruction. Whoever writes `test/eval/lane-aware-instrument-bucket.test.ts`, which the Definition of done names and which does not exist (confirmed 2026-09-11 and again 2026-09-12 by listing `test/eval/` — 29 spec files, no such name), should assert against `buildPermit` as well as against the bucket, or the green will be partial.

## Standing census: the two populations, and what this filter reaches

Six counts across seven passes, at widening scope. Where they disagree, the widest supersedes; the narrower ones are kept because they say how the figure was arrived at.

**The bucket's head is fully accounted for: 25 of 25 are recorded decisions, not un-done work.** The disqualifying failure mode this node names — a pass defaulting to `humans-required` because it could not think of a command — is **0 hits across 25 of 25**. *Superseded figures kept for method:* an early trace put it at 8 of 9 among entries that declared their reasoning, then 0 of 5 more among entries that did not, then 25 of 25. The intermediate "9 of 25 carry a declaration" figure was a **floor, not a rate** — its detector looked for a "Definition of done" heading and missed the `## Issues` form `ost_annotate` produces, so the true figure was at least 14 of 25.

The reasons take four forms: **14** carry the `## Issues` line "examined for a missing instrument and deliberately left without one"; **8** carry a "Definition of done — and it is not a command" with a bound humans-required bar; **2** ("Auto-read a file before the first write or edit to it in a session, instead of erroring", "Maintain a running per-item task list the next pass reads before reconstructing state itself") carry dated notes from repo-sighted passes establishing the artefact is the harness rather than this repository; and **1** ("Append-only tool surface with no delete or shell tool") carries a standing finding and an (a)/(b) supersession ask open since 2026-08-04 — a class this filter does not reach, since a spec asserting "no delete tool" goes red against today's code but would be measuring the deliberate absence of a shipped feature, which a builder could pick up and implement.

The eight entries whose sole test was traced to a set lane, individually: "Route the humans-required solution into the ask queue instead of dropping it from the instrument queue", "Group the queue by error signature at read time, and change nothing on disk", "A human-edited manifest of loop-prescribed call sequences the harvester suppresses", "Hand the oversized body to the harness's own compaction and store nothing new", "A repeated wait on the same condition resumes and doubles its budget automatically, up to a ceiling set once by hand", "An operator-set evidence window in ost.config.yaml, amended by hand like discovery.target", "Buy the answer from the operator once and print it on expiry, rather than building any way to infer it", "Adopt the platform's own job-control wait instead of maintaining a bespoke shell helper". All eight carry a written justification on the parent solution naming why an exit code cannot reach the belief — whether anyone trusts the groups enough to act from them, whether the founder keeps one more list current, whether the operator answers an ask that arrives with a clock on it, whether a compaction silently dropped a retraction.

The five that were unlabelled and reasoned, each examined by an earlier repo-sighted pass and each ending with the same request for `ost-agent lane --set`: "A highlight criteria note the founder edits and the loop reads before deciding what to surface" (viability about one person's behaviour over months), "Axioms elicited at the moment a derivation needs them, one accept-or-reject ask at a time" (viability about an answering rate), "Borrow the axiom set from a named body of practice and let the human only veto" (desirability about whether veto-only review confers ownership), "Monitor states its accepted command grammar up front rather than discovered by refusal" (the artefact is a harness tool no spec here can reach, and the belief is a counterfactual besides), "Name the specific mechanisms a hand process structurally cannot have" (its feasibility half is already spec-backed and green, so a spec here would re-prove what passes rather than fail today).

**The tail reads like the head: 0 of 3 sampled entries is un-done work.** The standing caveat that "45 entries were not pageable from this surface" was a property of one tool's display cap, not of the bucket: it is ordered by title, the shown 25 run A through R and end at "Run both for two weeks on the same evidence and publish what diverged", so the hidden entries are titles S through Z and a glob over the vault reaches them directly — 105 Solution files in that range, and 48 AssumptionTest files carrying no `instrument:`, no `lane:` and no `sight:`. Three were read in full, chosen as the most mechanizable-looking (a bias toward finding un-done work, so this is the conservative result): "Do the shipped sweeps actually find a planted instance" (12 plants, 12 found; `test/eval/planted-instance.test.ts` shipped in v0.20.0), "Test humans can promote while the agent is blocked from validating" (`test/security/self-validation.test.ts` asserts both halves of the mechanical threshold; what remains is "5 of 5 humans promote without friction", which no exit code observes), "Does refusing a newline inside a wiki-link catch breaks nothing else catches" (run 2026-07-26 over 100 commits of both vaults, all three pre-committed bars cleared; its parent solution is shipped). All three are one shape: **the mechanical half is already built and green**, so an instrument naming it would pass on arrival and the ruleset forbids setting it.

**The two populations are disjoint, and the one this filter cannot reach is 67.** Frontmatter census over every node file, run three times: 500 tests / 371 instrumented / 62 laned `humans-required` (2026-09-01); 512 / 379 / 66 (2026-09-08); 515 / 381 / 67 (2026-09-08, later — reconciling exactly with the rollup's independently computed "381 of 515 name a runnable command"). On every measurement the combined count equals instrumented + laned exactly, one field per file, so **no test carries both** — the `ost_set_instrument` refusal that is supposed to prevent that combination holds, 0 of 500 — and the residue carrying **neither has been 67 every time**. Those 67 are invisible to this filter and to `ost-agent verify` alike: nothing can run them and nothing records that a person must.

**The residue is inert, and it is the same 67 rather than a same-sized 67.** Across one week the tree added 12 tests and every one acquired either an instrument (8) or a lane (4); the residue gained nothing and lost nothing. Cardinality alone cannot distinguish that from churn, so the members were enumerated by name and checked against every one any prior firing had committed to writing: **all eleven are still in it** — "Pre-order probe - will anyone pay before the map proves itself", "Two-week recruiting test for interview supply", "Cold-offer test - will outside teams hand over real discovery work", "Ask five prospective operators whether they would let their vault report outward", "Interview ten solo builders about the last decision they made with nobody to check it", "Replay ten past runs and count how many needed a scope nobody would have predicted", "Count how much post-handoff work in past sessions would have survived a failing check", "Do the shipped sweeps actually find a planted instance", "Test humans can promote while the agent is blocked from validating", "Sweep both vault histories for writes that landed as undefined or empty", "Does refusing a newline inside a wiki-link catch breaks nothing else catches". Not one has acquired an instrument or a lane since it was first named. The mechanism keeping the set inert is visible in the same arithmetic: new tests are born with a runnable form or a lane, while the pre-lane commercial and interview branch is never revisited, because nothing lists it. Its own invisibility preserves it.

**2026-09-12 — the residue is enumerated in full for the first time, and none of the 67 is instrumentable.** Every prior count named 11 of the 67 and characterised the rest by title correspondence. This pass listed all 67 by name (one order-independent multiline frontmatter match over the whole corpus: `type: AssumptionTest` followed only by field lines that are neither `instrument:` nor `lane:`) and read every title. The composition is what the inversion below predicts and there is no fourth population hiding in it: **≈46 are outside-people measurements** (interviews, prospects, pricing probes, cold offers, recruiting supply, blind readers, operator time-and-motion — "Ask ten buyers what happened the last time a tool they relied on shut down", "Publish six pieces over six weeks and count strangers who arrive and try it", "Would operators accept unattended self-modification"); **≈14 name a human as the instrument in the title itself** ("Hand-compute unblock counts and see if the operator's pick changes", "Group the harvested tool errors by hand and see whether one rule reproduces the grouping", "Replay the three recorded failed runs through the journal-alert rule on paper", "Judge the eighteen reopened items — were they genuinely finished", "Hand-label the gated rows and check whether a detector agrees"); **4 are the already-answered class** whose mechanical half is green and therefore un-instrumentable by the red-now rule (the three sampled above plus "Sweep both vault histories for writes that landed as undefined or empty"); **1 is retired** (`status: deferred` — "Apply the escalating message to the five-failure session and check where it would have fired"); and **2 are longitudinal questions about whether a human behaviour persists** ("Do named unfixed thresholds actually get fixed", "Does a placeholder outcome get replaced, or does it become the tree's real root"), which no exit code observes however long it runs. **Candidates for an instrument: zero.** So the residue is not *mostly* human-measurement work — on a complete read of its titles it is entirely so, and the outstanding act is 67 `ost-agent lane --set` calls rather than a search for commands earlier passes missed.

**Two entries that looked mechanizable and are not, so nobody need re-check them.** "Replay ten past runs and count how many needed a scope nobody would have predicted" and "Count how much post-handoff work in past sessions would have survived a failing check" were read in full because their titles read as retrospective replays a spec could do. Neither is: each defines its measurement as a person's judgement over records that already exist — "ask whether a person writing the scope at dispatch time, knowing only the task, not the outcome, would have included every one" — and the second says outright that a human runs it.

**The inversion, which is the correction that matters.** An early reading had the sceptical residue sitting in the non-commercial entries and expected the commercial ones to be labelled. The lane census says the opposite. Every non-commercial entry traced is **labelled already**, its test one of the 62–67 on `outstandingAsks` under its own name — the sandbox ask for the background-output candidate, the two Monitor implementation asks, the editor-tool auto-read ask, the task-list ask, the differentia-grading ask. The **commercial** entries are the unlabelled ones: "Continuous story-based interview habit" rests on "Two-week recruiting test for interview supply", and "Run both for two weeks on the same evidence and publish what diverged" on "Have a second person run the hand arm, so the comparison is not built by its own author", and neither test carries a lane. So the unlabelled population is not a sceptical residue needing case-by-case judgement — it is a small, old, homogeneous branch ideated under `agent:P3_ideate` and ported before lanes existed, whose beliefs are recruiting, pricing and willingness to pay. Those are the least contestable humans-required calls on the tree, and a human can label them in one sitting without adjudicating anything. That also retires the "roughly 24% of the bucket recurs forever" estimate, which was drawn from the head before the residue was identified.

**What this does to the candidate, stated fairly.** A lane filter shipped today reaches the 62–67 labelled tests — the ones actively generating the recurring bucket — and **slightly under half** of the tests that can never produce an exit code. "Close to a complete fix" was measured against the head of 25 solutions; against the whole test corpus it is not. And the residue will not age out behind the filter: it is inert, so it would stay exactly this size indefinitely. That is a fact about sequencing rather than about merit, and it argues neither for nor against the one line.

**One stale instruction, worth fixing before it propagates further.** All 14 examination annotations close with the identical sentence: "What a human should do: set the lane with `ost-agent lane --set`, since `ost_flag_humans_required` is withheld on the unattended surface." For at least six of the 14 the lane is *already set* — the test is on the ask queue — so the sentence is boilerplate that has outlived its accuracy, and a human acting on it would be re-setting lanes that already exist. A future pass writing that annotation should check the test's frontmatter before including the line.

**The cheap move that is not a build.** The five reasoned-but-unlabelled entries named above each name the same command against themselves. Running `ost-agent lane --set` on them would move them into the population the filter reaches before any code is written, and would test the filter's premise on real nodes rather than on a fixture. That is a human's call and no pass has made it — the whole point of the withheld tool is that compute does not label its own way out of a queue.

## Method and limits, for the whole census

Counts are of files by frontmatter field, so a test whose lane or instrument was written into prose rather than frontmatter is counted as carrying neither. The disjointness claim is an arithmetic identity over three counts, not a per-file check that no single file holds both — a file holding both plus a file the `type:` match missed would preserve the totals and break it. The 67 counts *tests*, not solutions, so it does not convert directly into bucket entries: several may hang beneath one solution, and some beneath solutions already excluded for other reasons.

On the traces: the 25 entries were the title-ordered head, and the three tail entries were selected for mechanizability rather than at random, so they bound nothing about the other ~42 beyond removing the claim that the tail is unexaminable. Solution-to-test mapping was confirmed by individual trace for the eight labelled and two commercial entries and inferred from title correspondence for the rest — that inference is the weakest step and it runs toward *overstating* how many are labelled.

On the 2026-09-12 enumeration specifically: it is a complete list of the 67 and a complete read of their **titles**, not of their bodies. Two of the 67 were read in full and eleven had been examined by prior passes; the remaining ~54 are classified from their titles alone, which is enough to say a title names a person as the measurement and not enough to rule out a mechanical half hiding inside a body. The direction of that error favours the flattering answer — a body could contain a spec-answerable sub-question its title does not advertise — so "candidates for an instrument: zero" should be read as "zero visible from titles", and a human labelling the 67 will read the bodies anyway. The ≈46/≈14/4/1/2 split sums to 67 with some titles legitimately fitting two classes; it is a characterisation, not a partition.

On the standing finding: `src/eval/buildable.ts` probes at 23,611 bytes with `wouldTruncate: true` and cannot be served whole from this surface, so the `permitFrom` finding is first-party while the continued absence of a lane check in `solutionsMissingInstruments` itself is established behaviourally — each sweep lists solutions whose sole test carries `lane: humans-required` in frontmatter, which a lane-filtered bucket could not return. That is weaker than reading the guard's absence. The claim that `ost_set_instrument` refuses a cautious-lane test is read off the comment in `testsAwaitingVerification`, not off that tool's own source.

Nothing in any of this was executed: no test was run, no result recorded, no rung moved, no instrument set, no lane set, no status changed, no node created. It is observed structure of this vault and of the product's own code — it grounds feasibility, not desirability, and it says nothing about whether anybody outside this project wants a quieter bucket. `ost_check` is withheld on this surface, so these writes are unverified by the invariant checker by design.

## History
- 2026-09-12 body edited, dropping `## 2026-09-01 — the defaulted-label risk this node names, counted for the first time`, `## 2026-09-01 — the population this filter does NOT reach is roughly six times larger than the last count found`, `## 2026-09-01 (later firing) — the head is now fully accounted for, and the unreachable residue is a different population than either earlier count guessed`, `## 2026-09-01 (third firing) — the unlabelled population counted over all 500 tests instead of inferred from the head`, `## 2026-09-01 (fourth firing) — the 45 unpageable entries are pageable, and the tail reads like the head`, `## 2026-09-08 — the 67 has not moved in a week, so the residue is not draining on its own`, `## 2026-09-08 (later firing) — the limit the section above names as the one that matters is closed: it is the same 67, not a same-sized 67`, `## 2026-09-11 — the one-line fix is a two-site fix: `permitFrom` has the same blind spot and emits an instruction the tool would refuse` — Eight dated census sections had accreted here (four on 2026-09-01, two on 2026-09-08, one on 2026-09-11), growing the node to 38,730 characters, with seven of the eight opening with some version of "kept short, per this branch's convention" — the convention defeating itself. Five of them supersede each other on the same numbers (8 of 9 traced, then 0 of 5 more, then 25 of 25; 500/371/62, then 512/379/66, then 515/381/67; a 24%-residue estimate and the two sections that correct what the residue is made of), so a reader arriving today must read all eight in order to learn which figures still stand. Folding them into one standing census and one standing finding. No distinct claim, number, node title, trace, superseded estimate or limit is dropped — every one is reproduced, with superseded figures marked as superseded rather than deleted, and git holds the full prior text. This is the same repair three prior passes applied to "A human-edited manifest of loop-prescribed call sequences the harvester suppresses" and a 2026-08-21 pass applied to "Append-only tool surface with no delete or shell tool", for the same stated reason. This firing's own new work — the first complete enumeration of the 67-test residue, and the finding that none of them is instrumentable — is recorded inside the census rather than as a ninth dated section, deliberately, since adding one would be the exact pathology this edit repairs. The candidate's claim, its variation position and its Definition of done are unchanged.
