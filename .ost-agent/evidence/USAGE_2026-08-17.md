---
id: 'USAGE:2026-08-17'
source: 'USAGE:2026-08-17'
title: 'Usage trace 2026-08-17 — 506 calls, 3 failed'
timestamp: '2026-08-17T23:59:59.000Z'
actor: usage
---
# Usage trace — 2026-08-17 (506 tool invocations, machine-recorded)

Mechanical rollup of the append-only tool-invocation trace. Computed, not composed:
no agent narrated, selected, or summarized these numbers.

- **Calls:** 506 (503 ok, 3 failed)
- **Duration:** p50 47ms, max 374ms
- **Sessions:** 21

| Tool | Calls |
| --- | --- |
| ost_read_tree | 272 |
| ost_next_work | 134 |
| ost_ingest_inbox | 41 |
| ost_append_to_node | 24 |
| ost_annotate | 20 |
| ost_create_node | 13 |
| ost_edit_node | 2 |

| Surface | Calls |
| --- | --- |
| mcp | 506 |

**Failed calls (redacted, first 3):**
- `ost_read_tree`: no node on the tree carries that title. Titles are exact and come from ost_read_tree's own listing — call it with no arguments and use a `title` from `nodes` verbatim.
- `ost_read_tree`: no node on the tree carries that title. Titles are exact and come from ost_read_tree's own listing — call it with no arguments and use a `title` from `nodes` verbatim.
- `ost_read_tree`: no node on the tree carries that title. Titles are exact and come from ost_read_tree's own listing — call it with no arguments and use a `title` from `nodes` verbatim.

Evidence class: **observed behavior** — machine-recorded trace of tool invocations;
no narrator. Grounds usability and the agent-tool loop, not external demand.
