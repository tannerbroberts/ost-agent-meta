---
id: 'USAGE:2026-08-20'
source: 'USAGE:2026-08-20'
title: 'Usage trace 2026-08-20 — 313 calls, 3 failed'
timestamp: '2026-08-20T23:59:59.000Z'
actor: usage
---
# Usage trace — 2026-08-20 (313 tool invocations, machine-recorded)

Mechanical rollup of the append-only tool-invocation trace. Computed, not composed:
no agent narrated, selected, or summarized these numbers.

- **Calls:** 313 (310 ok, 3 failed)
- **Duration:** p50 23ms, max 2102ms
- **Sessions:** 5

| Tool | Calls |
| --- | --- |
| ost_read_tree | 127 |
| ost_read_repo | 58 |
| ost_next_work | 51 |
| ost_create_node | 40 |
| ost_append_to_node | 20 |
| ost_ingest_inbox | 10 |
| ost_annotate | 5 |
| ost_set_status | 2 |

| Surface | Calls |
| --- | --- |
| mcp | 313 |

**Failed calls (redacted, first 3):**
- `ost_create_node` on mcp: "Create an opportunity beside an existing sibling with no differentia and require the refusal, then supply one per sibling and require the write" cannot carry that instrument: `test/ost/sibling-differentia-guard.test.ts` does not exist in the configured product repo (OST-Agent), so its red would say
- `ost_create_node` on mcp: "A build that is finished and waiting in an open PR is picked as a target again, because selection reads only the tree" cannot declare 'observed': what it points at supports 'assertion'. The two measurement rungs assert that something was measured, and no byline confers them. declare 'assertion' or 
- `ost_create_node` on mcp: "A build that refutes my idea looks, to the loop, exactly like a build that is not finished yet" cannot declare 'observed': what it points at supports 'assertion'. The two measurement rungs assert that something was measured, and no byline confers them. declare 'assertion' or lower (demotion is neve

Evidence class: **observed behavior** — machine-recorded trace of tool invocations;
no narrator. Grounds usability and the agent-tool loop, not external demand.
