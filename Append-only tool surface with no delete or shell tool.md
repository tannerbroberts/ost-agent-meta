---
type: Solution
status: unvalidated
source: 'INBOX:2026-07-22-safety-requirement.md'
created: '2026-07-25'
evidence: assertion
---
#Solution #ported-from-ost-agent-vault #evidence/assertion
[[A full discovery pass can be completed with no delete and no edit tool]]

**Candidate solution (unvalidated).** The entire toolset offered to the agent is create / append / annotate / set-status only — there is deliberately no delete, edit, or shell tool. Destruction is impossible because the capability is absent, not merely discouraged.

**Approach:** *safety by construction* — remove the dangerous verbs.

**Contrast with siblings:** unlike the git-substrate solution (which makes harm revertible) this makes harm unexpressible in the first place; unlike push-off-by-default (containment) it addresses local action, not exfiltration.

_Addresses: "Fear the agent could take a destructive, irreversible action". Related to "Want proof no hijackable capability even exists". Unvalidated — human to review._

## History
- 2026-07-24 evidence: (none) → assertion — retro-labeled: sources are founder notes, the agent's own sessions, or model ideation — no external party involved; floor rung per the ladder's own rule
- 2026-08-05 unlinked "Test can a full pass be done with no delete or edit tool" — moved under "A full discovery pass can be completed with no delete and no edit tool" — the belief this test measures now has a node of its own

## Issues
- 2026-08-04 Possibly stale, and left un-instrumented for that reason. This solution's premise — that the tool surface has no delete or edit tool — no longer describes the product: `ost_edit_node`, `ost_detach_nodes` and `ost_merge_nodes` all exist and this pass used all three. Its test, "Test can a full pass be done with no delete or edit tool", therefore asks a question the product has already answered in the negative by moving on.

I did not write an instrument for it, because any command I chose would be measuring a design the product has left, and a red-today spec for abandoned behaviour is worse than no spec — a builder could pick it up and implement it. I also did not set it `deferred` or merge it away: whether the append-only surface was the right thing to leave is a product judgement, and the tree still carries live needs about irreversibility ("A call the tool should have refused is permanent, because append-only cannot take it back") that this node's reasoning bears on.

For a human: decide whether this is (a) superseded and should be `deferred` with a note pointing at what replaced it, or (b) still a live alternative, in which case its body needs rewriting to argue against the current surface rather than describe it. Flagged rather than actioned because both readings are defensible and choosing between them is not the sweep's call.
- 2026-08-05 2026-08-05 The 2026-08-04 staleness finding above is confirmed rather than merely repeated, and by the same means: this unattended sweep again used the mutating tools whose absence this node's premise asserts. `ost_edit_node`, `ost_detach_nodes` and `ost_merge_nodes` are all on the sweep's granted surface, and the sweep's own instructions now state the replacement rule outright — the vault is no longer append-only, and what replaced it is a narrower guarantee: reserved sections (`## Results`, `## Uncovered`, `## Instrument Log`) can be neither authored nor destroyed, `## History` stays append-only, retraction is a human's CLI call, and git holds every write. So the product did not abandon safety-by-construction; it moved the construction from "no dangerous verbs exist" to "the verbs that would let an agent forge or revoke a human's finding do not exist". That is a materially different claim from the one this node makes, and it is arguably the stronger one, since the previous surface's own failure is already recorded in this tree as "A call the tool should have refused is permanent, because append-only cannot take it back". I am leaving the node un-instrumented and un-deferred for the reason the prior pass gave — a red-today spec for abandoned behaviour is worse than no spec, because a builder could pick it up and implement it — and repeating the ask, now with the successor named: a human should either defer this with a pointer to the reserved-section guarantee that replaced it, or rewrite its body to argue that removing the verbs entirely was better than constraining which ones exist. Two passes have now declined to make that call, which is itself worth knowing.
- 2026-08-06 Stale as titled. This solution proposes a surface with "no delete", and the surface it describes no longer exists: `ost_edit_node`, `ost_detach_nodes` and `ost_merge_nodes` all ship, and `ost_merge_nodes` deletes a file outright. The ruleset now directs passes to merge duplicates in preference to annotating them. The full argument, with the threshold it misses, is recorded on the test beneath it — "Test can a full pass be done with no delete or edit tool".

The half of this solution that did survive is worth keeping and is not what the title says. What the surface still refuses is specific: no tool can author or destroy `## Results`, `## Uncovered` or `## Instrument Log`; `## History` is append-only and corrected by appending; retraction is a human's CLI call; there is still no shell tool. The invariant that held is "compute may not create or destroy the record that something happened outside the tree" — which is a claim about evidence, not about deletion, and it is the one that has actually been load-bearing.

Recommend a human either rewrite this node around that narrower claim or defer it. This pass did neither: rewriting it would silently re-aim a candidate that a human chose, and deferring it would record an abandonment nobody decided on. Flagging is as far as the unattended surface should go.

Related and untouched: "Refuse a proving command whose exit code cannot report failure" and "Refuse a write whose content is empty or literally undefined" are siblings of the same era and may have shipped too. Neither was checked this pass, because the product repository is not readable from here — `product.repos` is unconfigured, which is recorded separately as its own opportunity.
- 2026-08-06 2026-08-06 Half of this node's claim has been overtaken by the product, and a human should decide what remains — flagged rather than repaired because the two halves point opposite ways.

This sits in `solutionsMissingInstruments`, and no honest instrument can be written for it as worded. Its assumption is "A full discovery pass can be completed with no delete and no edit tool" and its test is "Test can a full pass be done with no delete or edit tool". The product has since answered that question in the negative by building the tools: the ruleset this vault's agents operate under now states plainly that "The vault is no longer append-only", and `ost_edit_node`, `ost_merge_nodes` and `ost_detach_nodes` are on the unattended surface — this pass used all three families of write. A spec asserting a pass can complete with no edit tool would now be asserting something the product deliberately stopped believing.

The other half of the title is still true and still shipped: there is no shell tool and no delete tool on the agent surface, and the confinement that provides is intact. So this is not simply stale.

What a human needs to settle: whether this node is (a) `deferred`, because the append-only design it argues for was abandoned on purpose, with the surviving no-shell-tool claim re-homed on a node of its own; or (b) `shipped` in a narrowed form, rewritten to claim only the confinement that actually holds. Either is a one-call repair once the call is made. This pass will not make it, because "the product changed its mind about this design" is a judgement about intent, and the node's own status would be evidence in whichever direction it was set.
- 2026-08-06 2026-08-06 This solution's premise has been overtaken and the node does not say so. The vault is no longer append-only: `ost_edit_node` rewrites a node's prose, `ost_detach_nodes` removes an edge, and `ost_merge_nodes` folds a duplicate away and deletes its file. This pass used all three. What replaced the blanket rule is narrower and arguably stronger — the reserved sections `## Results`, `## Uncovered` and `## Instrument Log` cannot be authored or destroyed by any tool on this surface, `## History` remains append-only, `validated` is not a status the agent can set, and retraction is a human's CLI call. So the property the node was protecting is still held, by different means. Two things follow that a human should judge rather than an unattended pass. First, the node's title now describes a surface that does not exist, and it is being reported as needing an instrument for behaviour that has been deliberately changed — any spec asserting "no delete tool" would fail today, which looks like a valid red but would be measuring the absence of a feature that was intentionally added. Second, this node is the standing argument against "Refuse an instrument that passes on arrival", which proposes adding command execution to a write path; that objection deserves to be re-stated against what the surface actually guarantees now, not against append-only. Left for a human because deciding what this product is willing to be is not an unattended call.
- 2026-08-07 Premise superseded by the product — flagged for human review, not repaired. This solution proposes an append-only surface "with no delete or shell tool", and its assumption beneath it ("A full discovery pass can be completed with no delete and no edit tool") is tested by "Test can a full pass be done with no delete or edit tool". The vault is no longer append-only: the current agent surface includes `ost_edit_node` (replaces prose), `ost_detach_nodes` (removes an edge) and `ost_merge_nodes` (folds a duplicate away and deletes its file). What survived of the original idea is narrower and different in kind — the reserved sections `## Results`, `## Uncovered` and `## Instrument Log` can be neither authored nor destroyed by any tool, and `## History` is still append-only. This pass deliberately did NOT attach an instrument here. The test's question as written ("can a full pass be done with no delete or edit tool") is now answered in the negative by the product's own design, and writing a command for the surviving narrower claim would be inventing a new question under an old node's identity rather than giving the existing question a runnable form. A human should decide whether to re-word this solution to the reserved-section invariant it became, or mark it `deferred` as overtaken.
