---
type: Assumption
source: 'TRANSCRIPT:748498c4-31fb-4110-9012-464c441a463f'
created: '2026-08-06'
evidence: assertion
---
#Assumption #unvalidated #evidence/assertion

**The belief, stated so it could be false.** An inventory specific enough to have prevented the observed path failures fits in a startup context budget.

**Why it is the riskiest one.** The two requirements pull against each other and this vault has the receipt for the losing side: `ost_read_tree` was refused at 134,240 characters for one tree of 1,007 files. The failures this solution addresses are file-level — `sed: src/cli/index.ts`, `ls: test/adapters/transcript-model-reader.test.ts` — so a directory-level summary would not have prevented either, and a file-level listing of a repository plus a 1,007-node vault is the size that already got refused. If the useful resolution does not fit, the solution collapses into a coarse inventory that costs context on every run and prevents none of the recorded cases.

**What class this is.** Feasibility, and it is measurable today by generating the thing and counting tokens.

**How it could come out false — usefully.** A scoped inventory might clear the bar: only the test suite's layout, only the directories a run is granted, only depth two. The test below is what finds where the line is, and a narrower solution that fits is a better outcome than a broad one that does not.
