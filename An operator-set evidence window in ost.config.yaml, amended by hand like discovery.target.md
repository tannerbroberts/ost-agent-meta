---
type: Solution
source: 'agent-run:unattended-sweep-2026-08-27'
created: '2026-08-27'
evidence: assertion
authorship: machine
---
#Solution #unvalidated #evidence/assertion
[[An operator who must move the window by hand will actually move it more than once]]

**Variation dimension: who-does-the-work. Position taken: the person, not the agent.**

The vault already has exactly this mechanism for one queue. `discovery.target` in `ost.config.yaml` is human-set, has no tool that can write it, and when it is present `ost_next_work` scopes the whole sweep to that branch and reports what the scoping excluded. This solution says: do the same thing for the other capped lists. Add an operator-held window declaration — a prefix, an id range, a date range, or an explicit list of titles — and the firing is shown that slice and told the size of what it was not shown.

**What it deliberately does not do.** It builds no selection policy. There is no rotation, no recency stamp, no cursor, no sampling. The machine never decides which 25 of 419 it looks at, because on this account choosing where a paid firing spends its attention is a prioritisation decision, and prioritisation is the operator's throughout this product. The agent's job is to work the slice well and to report the denominator honestly.

**What it gives up, stated plainly.** Coverage becomes a function of the operator remembering. An operator who sets the window once and never amends it has reproduced today's defect exactly, with the difference that the freeze is now visibly theirs rather than the surface's. That is the trade: the sibling candidates buy guaranteed advance at the cost of the machine choosing; this one keeps the choice human and accepts that an unattended tree can stall on an unmoved cursor. It is the weaker answer to coverage and the stronger answer to authority, and which of those matters more is not something this node can settle.

**Cheapest form.** One config key, read where `discovery.target` is already read, applied as a filter before the cap rather than after it — so the window selects from the whole set and the cap then trims the window, instead of the cap trimming an unfiltered head. Reported alongside the existing `shown`/`total`/`hidden` so a reader can tell a windowed 25 from a truncated 25.

Ideated by an unattended pass on 2026-08-27 against the assigned dimension. Not blind: this surface holds no grant to run independent parallel ideators, so all three candidates under this opportunity were composed in one context by one author, which is the condition the blind-ideation rule exists to prevent. Read the three as one author's three answers, and discount their apparent distinctness accordingly.

## Definition of done

"Count how many times the operator amends discovery.target over eight weeks of git history"

No command: this one is humans-required on purpose. The bar is at least 3 distinct operator-authored changes to `discovery.target` over 8 weeks, read out of the vault's own git log — the bet is already running in production and the evidence is already on disk, so this is the cheapest of the three tests to settle and should be settled first. A refuted verdict here retires this candidate for the price of reading a log, before either sibling is built.

## Issues
- 2026-08-28 2026-08-28 unattended sweep, observed first-party in this pass's own `ost_next_work` response: with `evidence.staleAfterDays` unset, the summary renders the literal string "captured more than undefined day(s) ago" — `undefined` interpolated into operator-facing prose. The per-row `mirror.note` on the same response handles the unset case correctly ("no evidence.staleAfterDays is set, so nothing here calls that too old"), so the two renderings of the same fact disagree and only the summary is broken. Small and cosmetic, but it sits on the exact config key this candidate proposes to introduce, and it is evidence about that key's current handling rather than about this candidate's merits: whoever builds an operator-set window here inherits a formatter that already does not guard the unset branch. Not instrumented and no node created — recorded as a repo-grounded observation for whoever picks this up. This annotation does not bear on the humans-required Definition of done above, which stands unchanged.
- 2026-08-29 2026-08-29 unattended sweep: the `undefined` formatter defect recorded in the 2026-08-28 note above is still live and reproduced verbatim in this firing's own `ost_next_work` summary — "captured more than undefined day(s) ago" — one day later, with the per-row `mirror.note` on the same response still rendering the unset case correctly. Recording the reproduction rather than restating the finding, because a defect observed once could be a transient and a defect observed on two consecutive days from two separate firings is a standing one. Nothing else about this node changed: it was re-read in full this pass while censusing `solutionsMissingInstruments`, its humans-required Definition of done stands unchanged, and it is counted in that census under the longitudinal kind. No instrument was set and none should be — this node says so on itself.
- 2026-08-29 2026-08-29 unattended sweep (second firing of the day) — a NEW observation bearing on this candidate's merits, not a restatement of the two `undefined`-formatter notes above. Observed first-party in this pass's own `ost_next_work` response: `unmappedEvidence` reported 457 total and showed 25, and all 25 shown carried `actor: "transcript"` — the vault's own session-friction records. The cap trims an unfiltered head, exactly as this node's "Cheapest form" paragraph describes, and the head is monopolised by the one channel that generates records fastest. The consequence is concrete and is the strongest first-party argument this candidate has yet acquired: the rollup credits other channels with sources (`channel:inbox` ×4 among them), so if any human-authored inbox note or deposit is sitting unmapped in the 432 hidden records, it is unreachable from this surface — a firing cannot page past the cap, and the highest-value evidence class in the tree is therefore starved by the lowest-cost one. That is the defect this node proposes to fix, observed happening rather than predicted. It cuts both ways and both should be weighed: it strengthens the case for a window, and it sharpens this node's own stated give-up ("coverage becomes a function of the operator remembering"), because an operator who never amends the window would freeze the head permanently instead of merely leaving it unsorted. No node created and no instrument set — this node's humans-required Definition of done stands unchanged, and this pass declined to ideate a sibling because selecting which slice a firing sees is a prioritisation decision and prioritisation is the operator's throughout this product.

## 2026-09-01 — this node's strongest argument checked against the disk, and it does not hold up

Kept short. Only what is new, and it cuts against this candidate rather than for it.

**The claim being checked.** The 2026-08-29 annotation above is this node's sharpest first-party argument: the cap trims an unfiltered head, the head is monopolised by the transcript channel, so "if any human-authored inbox note or deposit is sitting unmapped in the hidden records, it is unreachable from this surface — and the highest-value evidence class in the tree is therefore starved by the lowest-cost one." That was stated as a conditional and the condition was never tested, because no earlier pass could page past the cap. This pass could: the vault is its own working directory, so the evidence store is readable directly rather than through the sweep's 25-row window.

**What the disk says.** The vault holds **52 `INBOX_*` evidence records**, spanning 2026-07-22 to 2026-08-20 — founder theory, doctrine notes, market scan, external review, build-loop friction. Against them, roughly **180 node files carry an `INBOX:` source**. The four most recent notes, which are the likeliest to be stranded because nothing has had time to map them, are each cited by several nodes: `2026-08-16-audit-loop-efficiency-and-checkout-drift` by six, `2026-08-16-` and `2026-08-19-build-loop-stuck-…` by five between them, and `2026-08-20-build-loop-stuck-every-run-records-the-tool-surface-it-actually-had` by three.

**So the starvation is not happening, and the queue is one channel deep.** The 533 unmapped records are transcript friction; the human-authored channel is mapped, including its newest arrivals. The consequence for this node is specific: its 2026-08-29 argument concluded that a window would rescue high-value evidence the cap was hiding, and on this check there is no such evidence to rescue. What the cap hides is more of the same self-generated friction — which the branch beside this one has now censused three times and found to contain zero new needs.

**What survives, stated fairly.** The mechanical defect the 2026-08-29 note observed is untouched: the cap does trim an unfiltered head, the head is monopolised by the fastest-producing channel, and a firing cannot page past it. That is still true and still worth fixing. What this check removes is the *urgency* argument — the case for a window rests on legibility and on operator authority over where attention goes, not on rescuing starved human evidence, because none is starved. Whoever weighs this candidate should weigh it on the grounds in its own body and discount the 2026-08-29 escalation.

**Limits.** Citation was confirmed individually for the four most recent notes, not for all 52; the ~180 figure is a count of node files carrying an `INBOX:` source, not a proof that every one of the 52 is cited at least once, and an older note could still be uncited without this check seeing it. "Mapped" here means a node names the record as its source, which is the same predicate the sweep uses, so this inherits whatever that predicate gets wrong. Nothing was executed, no rung moved, no instrument set, no status changed, and this node's humans-required Definition of done stands unchanged.

_Method: `Glob` over `.ost-agent/evidence/` and `Grep` over this vault's node frontmatter and bodies. Observed structure of this vault, read first-party._

## 2026-09-01 (later firing) — the check above looked at one channel and concluded about all of them; a second channel is 25 records deep and cited by nothing

Kept short, per this branch's convention. Only what is new, and it corrects the section immediately above rather than adding a census.

**The claim being corrected.** That section checked whether human-authored evidence was starved behind the cap, found 52 `INBOX_*` records all cited, and concluded: "The 533 unmapped records are transcript friction; the human-authored channel is mapped, including its newest arrivals… on this check there is no such evidence to rescue." It then withdrew this node's urgency argument on that basis. The check was sound for the channel it examined and the conclusion was drawn about the queue as a whole. Those are different sets.

**What the disk says.** The vault holds **31 `USAGE_*` evidence records**, 2026-07-25 through 2026-08-31 — machine-recorded rollups of the append-only tool-invocation trace, a channel distinct from both the inbox and session friction. **Six of the 31 are named as `source:` in node frontmatter** (07-26, 08-02, 08-03, 08-04, 08-05, 08-31), by nine nodes between them. **Twenty-five are named by nothing.** Under a looser predicate — the date appearing anywhere in any node body — twelve are reached and nineteen are not. Either way the channel is largely uncited, and no census on this tree has counted it.

**Why every pass missed it, which is the part that bears on this candidate.** `unmappedEvidence` is id-ordered, and `USAGE:` sorts after `TRANSCRIPT:`. With 519 of 544 rows hidden behind the cap, the entire usage channel sits in the tail where no firing has ever been able to look. The section above characterised the queue from the 25-row head plus a targeted check of the one other channel it happened to know about — so the conclusion that discounted this node was itself produced through the window this node proposes to fix. That is not a rhetorical point: the failure mode is a pass confidently describing a queue it can only see the front of, which is precisely the defect, observed on the tree rather than predicted.

**What this does NOT restore, stated plainly, because it cuts against the node.** Three uncited records were read in full this pass — 2026-08-16, 2026-08-23, 2026-08-30 — and none carries a need this tree lacks. Their content is read-heavy call mixes (reads run 76–87% of calls on those three days) and a handful of correctly-refused writes, and both signals are already mapped: "A third of my calls go on re-asking what is outstanding" and "The claim a node makes is buried under every pass's notes about it, so reading one node costs what reading ten should" both cite usage traces already, and "Two thirds of my calls failed, and each one only told me after I made it" holds the refusal signal. So the *urgency* argument stays withdrawn on content — there is still no starved high-value need waiting to be rescued.

**What it does restore, which is the argument this node actually rests on.** Legibility and operator authority over where attention goes. A whole evidence channel went 25 records deep with nothing naming it, across many firings, and no pass noticed — not because the records were judged and set aside, but because the surface never showed them and the one check that went looking generalised from a different channel. That is the concrete instance this node's "Cheapest form" paragraph describes, and it is the first one on the tree. A reader should weigh this candidate on that, not on rescue.

**Limits.** Citation was tested by frontmatter `source:` and by a whole-corpus body search for the `USAGE:<date>` string; a node that maps a record without naming it either way would be missed, and that error runs in the direction of overstating how many are uncited. Three of the 25 were read in full, chosen as the most recent and two mid-range — not at random, and not a rate over the channel; a need could sit in the other 22. The count is of records on disk, which is a different denominator from the 544 the sweep reports, and the two should not be differenced. Nothing was executed, no rung moved, no instrument set, no status changed, and no node was created.

_Method: `Glob` and `Grep` over this vault's own `.ost-agent/evidence/` and node frontmatter, and three evidence bodies via `ost_next_work({evidence})`. Observed structure of this vault, read first-party; it grounds feasibility, not desirability._
