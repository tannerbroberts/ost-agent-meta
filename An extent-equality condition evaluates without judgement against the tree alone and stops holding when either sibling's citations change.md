---
type: Assumption
source: 'agent-ideation:2026-08-21-unattended-sweep'
created: '2026-08-21'
evidence: assertion
---
#Assumption #unvalidated #evidence/assertion
[[An extents-unchanged ledger condition parses, holds while both siblings' extents match their recorded digest, and stops holding when either gains a citation]]

**Kind: feasibility.** The belief: the suppression ledger's contract — every condition is a boolean over the tree alone, refuses prose, and revives the item when the fact flips — can be met by a condition over two nodes' evidence extents, using the extents `evidenceExtents` already computes. Stated so it could be false: if the digest has to be recomputed over the whole forest on every `ost_next_work` call, the condition may cost more than the flag it silences on a wide tree; and if the extent of a sibling legitimately changes on every pass (a transcript channel adding citations beneath it), the condition never holds long enough to matter.

**Not covered here and named on purpose:** whether a human ever runs the CLI command that writes the ledger entry. The ledger's write path is human-only by its own spec, so this solution turns churn into an ask, and 49 asks in this vault currently have no recorded answer. A green spec proves the condition works; it says nothing about whether anyone will set it.
