---
type: Assumption
created: '2026-08-18'
evidence: assertion
authorship: machine
---
#Assumption #unvalidated #evidence/assertion

Usability belief: an agent will actually keep marking items in-progress/done throughout a run rather than only writing the list retrospectively at the end — the whole value of this solution depends on the list being current at the exact moment a session gets interrupted, which is also the moment it's least likely to be mid-update.

## Issues
- 2026-09-20 2026-09-20 unattended sweep — this Assumption carries NO AssumptionTest beneath it, and no bucket reports that. Found while tracing why its parent solution, "Maintain a running per-item task list the next pass reads before reconstructing state itself", sits in `solutionsMissingInstruments`. That solution has two assumptions: this one, and "A task list a prior session left behind saves more than it costs, and won't get acted on when stale or wrong", whose test "Ask the operator whether they'd trust a prior session's task list over reconstructing state themselves" carries `lane: humans-required` and is on the ask queue. So the solution's bucket entry is fully explained by the laned test — but this belief, that an agent keeps marking items through a run rather than writing the list retrospectively at the end, has nothing testing it at all. `ost_next_work` cannot surface this: it reports solutions missing assumptions, never assumptions missing tests, so an untested belief beneath a solution that already has one tested belief is invisible to every sweep. WHY NO TEST WAS CREATED HERE, deliberately: a repo-sighted pass already established for this solution that the artefact is the harness rather than this repository (recorded in the standing census on "The bucket gains the lane filter the function two doors down already has"), so no spec in `product.repos` can settle whether a session updates a list as it goes. A test written here today could therefore name no instrument, and `ost_flag_humans_required` is withheld on the unattended surface — so it would be created with neither field and would grow the neither-instrument-nor-lane residue, measured at 67 and unmoved across four counts, by one. Creating it would make the tree worse in the exact way that residue documents. WHAT A HUMAN CAN DO: create the test and set its lane with `ost-agent lane --set` in the same sitting, or record here that this belief is deliberately carried untested and why. Either is a decision; the present state is the absence of one. Nothing was executed, no node created, no rung moved.
