---
type: AssumptionTest
source: 'agent-ideated:2026-08-19-unattended-sweep'
created: '2026-08-19'
evidence: assertion
lane: humans-required
---
#AssumptionTest #unvalidated #evidence/assertion

Threshold: the answer names whether any file or record already tracks "target X failed its instrument on firing Y" across runs, and if so why it did not prevent the three re-selections observed on 2026-08-16 and 2026-08-19.

A person outside the building is the measurement here: Answering this requires reading the build loop's source and its persisted state directory (e.g. `.local/state/ost-build-loop/`) in the product repository; this unattended maintenance pass holds no repo-read grant, so a person with that access must check and report back.

## Issues
- 2026-08-22 2026-08-22 (unattended sweep, repo sight held) — THE REPOSITORY ANSWERS BOTH HALVES, including the "and if so why it did not prevent the re-selections" clause. The node's stated reason for the humans-required lane is "this unattended maintenance pass holds no repo-read grant"; this pass held `ost_read_repo`. DOES A PER-TARGET FAILURE RECORD EXIST ACROSS FIRINGS? No. The persisted state directory (`$OST_BUILD_STATE`, default `~/.local/state/ost-build-loop`) holds exactly: `last-fired` (a cadence timestamp), `lock/` (a mkdir mutex plus a pid file), `last-report.txt`, and four candidate scratch files — `candidates.txt`, `buildable.txt`, `pending-verification.txt`, `instrumented.txt`. Every one of the four is truncated with `: >"$FILE"` at the top of each firing. Nothing in that directory survives a run, and nothing in it is keyed by target. The only cross-firing record of an instrument is `## Instrument Log` on the test node itself, written by `ost-agent verify` — and that records an OBSERVATION (red/green/no-spec), not a build attempt. Nothing anywhere records "target X was selected on firing Y and the build did not land." WHY THAT DID NOT PREVENT THE RE-SELECTIONS — and this is the part the node asked for. Re-selection is not a memory failure; it is the designed behaviour of a permit that never expires. `permitFrom` clears a permit for any solution with an instrumented test satisfying `observedRed(t) && !observedGreen(t)`. A red observation is append-only and permanent, so that predicate stays true on every subsequent firing until something turns the instrument green. Meanwhile `testsAwaitingVerification` explicitly skips anything already observed — "A test that has already been observed — red or green — is not here. Re-running a red instrument every hour would burn the suite" — so the loop will never re-run it either. The two rules compose into a standing invitation: a target whose build fails (or is abandoned mid-way) keeps its red, keeps its permit, and is re-offered every firing, indefinitely, with nothing recording that it was already tried. That is a sufficient mechanical explanation for the three re-selections observed on 2026-08-16 and 2026-08-19 without needing any additional defect. ONE COMPOUNDING FACTOR worth naming alongside it: `buildableSolutions` does not filter on `status` at all, so even marking such a target `deferred` would not remove it from the candidate list — see the sibling ask about status filtering, which this pass answered from the same source. WHAT IS STILL A HUMAN'S: whether the fix is an attempt ledger, a permit expiry, a status filter, or `confirmPermit` on the hot path is a design choice among existing siblings, not this surface's call. Re-laning is `ost-agent lane "Ask someone with the build loop's source and persisted state open whether a per-target failure record already exists across firings" --set compute-only`. NOT ACTING: no lane changed, no result recorded, no instrument set. Source: first-party `ost_read_repo` of `src/eval/buildable.ts` and `examples/automation/build-pass.sh`. Nothing executed; rung unchanged.
