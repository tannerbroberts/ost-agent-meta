---
type: Assumption
status: unvalidated
source: >-
  tree-restructure:2026-08-05 — the belief this solution's test was already
  measuring
evidence: assertion
---
#Assumption #unvalidated #evidence/assertion
[[Load the machine deliberately and check that the calibration ratio holds while the raw number does not]]

The ratio is stable only if load moves both numbers together. If they respond differently, the gate now fails unpredictably instead of predictably — a worse position than an absolute number, which is at least wrong in a direction people understand.
