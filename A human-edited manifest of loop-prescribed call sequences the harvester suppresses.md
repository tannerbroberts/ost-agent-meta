---
type: Solution
source: 'TRANSCRIPT:98dcaba0-5cd8-4e56-8360-55b58a655cd8'
created: '2026-08-26'
evidence: assertion
authorship: machine
---
#Solution #unvalidated #evidence/assertion
[[The founder will keep a manifest of prescribed call sequences current as the loop's steps change]]

**Variation dimension: automated-vs-manual. Position taken: the matching is automated, the definition of "procedure" is deliberately left manual.**

A small declarative manifest lists the call sequences that are procedure rather than pain — `ost_ingest_inbox` followed by `ost_next_work` at the close of a pass; a blocking `TaskOutput` on a task the same session started. The harvester matches every candidate friction event against it and suppresses the matches automatically, with no judgement at harvest time. What stays manual is the list itself: a person edits it, because what counts as prescribed is set by the ruleset the person writes, and the ruleset changes.

**Why the manual half is the point, not a shortcut.** The loop's procedure is not a property of the code — it lives in the skill text and the firing prompt, both of which a human rewrites. Any mechanism that infers "prescribed" from the code would be reading the wrong artifact and would go silently stale the next time the loop's steps change. Making the list an artifact a person edits keeps it in the same place as the thing it describes, and a manifest entry that no longer matches anything is visible in a way an inference is not.

**Against the siblings.** Unlike deleting the `retry` class, this keeps genuine retry-after-failure signal intact — a repeat that is not on the list still files. Unlike adopting the host's error flag, the suppression rule is authored here and can encode this product's own procedure, which no upstream signal knows anything about.

**What it costs.** A maintenance burden with a known failure mode: this project's own tree already records that artifacts asking for recurring founder input are the ones that go unmaintained, and there is a live assumption elsewhere on exactly that question ("The founder will actually maintain a highlight criteria note over time"). A stale manifest fails in the dangerous direction — it keeps suppressing a sequence that has stopped being procedure, so real friction goes missing rather than merely unfiltered.

**What would make this the wrong pick.** If the prescribed sequences turn out to be few and stable, a manifest is ceremony around two hard-coded rules. If they turn out to be many and fast-changing, nobody will keep it current. It is the right pick only in the middle case.

Unvalidated — a human to review.

## Definition of done — and it is not a command

"Count how many recurring-input artifacts the founder has actually kept current, before asking for another"

There is deliberately no instrument here. The belief this candidate rests on is whether one specific person keeps one more list current, and the bar is: at least 2 of the founder's existing recurring-input artifacts were edited within 30 days of the change they were meant to track. A script can gather the timestamps that start the conversation; it cannot supply the half that decides the answer, which is why an artifact that went stale went stale.

The test title is quoted rather than wikilinked on purpose: its one backlink belongs to its parent assumption.

**Sequencing this candidate against its siblings:** this is the cheapest of the three to build and the only one that can fail after it ships, silently, by suppressing a sequence that stopped being procedure. So the viability answer should come before the build, not after — and if it comes back below the bar, the sibling that deletes the repeat inference outright is the default, because it asks nobody for anything.

**Lane note for a human:** this test was created in the humans-required lane at creation time. `ost_flag_humans_required` is withheld on the unattended surface, so if the lane ever needs changing it is `ost-agent lane --set`.

## Standing finding: the generator, read off the code

Established 2026-08-28 and confirmed by every sweep since. This node was ideated from the theory that the harvester counts prescribed procedure as friction. It is no longer theory — the mechanism has been read first-party out of `src/adapters/transcript.ts` and observed firing on record after record.

**The mechanism.** `extractFriction` builds a signature as `` `${name}:${JSON.stringify(input)}` ``, keeps a `seenCalls` set, and pushes a `retry` on the second appearance of a signature — with no reference to any `tool_result`, `is_error`, or outcome of the first call. `ost_ingest_inbox` and `ost_next_work` both take no arguments, so every call either makes has the identical signature `{}`. The unattended loop's own prompt prescribes calling both at step 1 and re-calling both at step 5. The second pair is therefore guaranteed to trip the repeat detector: the procedure the firing was instructed to follow is the generator.

**And a clean run does not stay silent.** `TranscriptSource.fetchSince` contains `if (events.length === 0) continue`, so a session with no friction produces no evidence item at all. A firing that followed its instructions and hit nothing wrong therefore does not produce nothing — it files a document titled "Session friction" whose entire contents are the loop obeying itself. Pure instances observed, thirteen to date: `6d96802b` (2 events), `ec918ca1` (5), `29ee50b7` (2), `01e55025` (2), `0e24ead9` (2), `0ffc6652` (1), `030e5db3` (5 retries, the highest retry count in the 2026-08-30 sample, every one prescribed), `7c2eebfc` (2), `8e77fa17` (3), `231e0b7a` (1), `99190d4a` (1), `fe37c31f` (1), and `6627cbf8` (1, alongside one denial).

**The generator's yield is per-iteration, not per-firing — established 2026-09-08 from `8e77fa17`.** Every pure instance before it carried the prescribed pair *once*: one `ost_ingest_inbox` retry and one `ost_next_work` retry, matching the loop's step 1 and step 5. `8e77fa17` carries `ost_ingest_inbox` twice and `ost_next_work` once. Read against the second-appearance rule above, that is 3 calls to `ost_ingest_inbox` and 2 to `ost_next_work` in one session. The loop's step 5 prescribes re-ingesting on *every* iteration before re-sweeping, so a firing that goes round twice mints two ingest retries rather than one. The yield is therefore `calls − 1` per argument-less tool, growing linearly with how many sweep iterations a firing performs.

**The design consequence, which is the builder-useful part.** A manifest that describes the prescribed sequence as *a pair* — ingest-then-sweep, matched once — under-suppresses exactly the firings that do the most work, and does so silently: the second iteration's ingest retry survives the filter and files as friction. The rule this candidate needs is count-independent — suppress *any* no-argument repeat of those two signatures within a session, however many times it recurs — rather than positional or paired. That costs nothing extra to build and is easy to get wrong from the records alone, because most pure instances are single-iteration firings.

**And the formula has no floor of two — established 2026-09-08 from `231e0b7a`, replicated twice since.** That record is a single `retry` on `ost_ingest_inbox` `{}` with zero `tool_error`: a firing that called `ost_ingest_inbox` exactly twice and `ost_next_work` once. It is the smallest record the prescribed pair can produce while still producing one, confirming the per-iteration reading from the bottom end — the earlier instances established that the yield rises with iterations, and this establishes that it does not start at two. A record can consist of a single line of the loop obeying itself. `99190d4a` (2026-09-09) and `fe37c31f` (2026-09-11) are the same shape exactly, so the floor case is now three records rather than one and is the modal pure instance rather than a curiosity.

**The detector is wrong in both directions, which bounds any rule built here.**

- *False positives — procedure read as friction.* The case above. Suppressible, and the no-argument condition is what makes it safely suppressible.
- *False negatives — real repeats the detector never sees.* `TRANSCRIPT:60eba32c` is 9 events, all `tool_error`, **zero retries**, six of them `await: gave up after 300s`. Since `seenCalls` spans every `tool_use` block and ignores outcomes, zero retries means no two calls shared an input: the session asked one question — has the suite finished yet — six times over 1800s and registered as a repeat exactly zero times, because the caller incremented its output filename between attempts. Detector recall on that record is 0 of 6. The consequence for that branch is recorded on "A repeated wait on the same condition resumes and doubles its budget automatically, up to a ceiling set once by hand".
- *Must-not-suppress — argument-carrying repeats that are genuine signal.* `cf2cef94` (2 retries, both re-issues of a wait that had just expired, from which a pass distilled a new Opportunity) and `09ec7cd2` (a `retry` on `Write` with a full argument payload, a real repeat of a write to the build loop's report file). Any rule must suppress the no-argument prescribed pair without taking these with it. That distinction is pinned by the sibling test "A repeated no-argument call with no error files nothing, while a repeat whose result is an error still files".

## Standing census: what the corpus is made of

Three counts were taken across passes at widening scope. Where they disagree, the widest supersedes; the narrower ones are kept because they say how the figure was arrived at.

**Whole-record self-manufactured noise — 76 of 534 (14.2%), full corpus, 2026-08-31.** Counted over every transcript record on disk via `Glob`/`Grep` on `.ost-agent/evidence/`, from each record's own header parenthetical: 78 of 534 carry no `tool_error` at all, of which exactly 2 (`da806545`, `414d3daa`) carry an argument-bearing retry and are therefore genuine. *Superseded estimate, kept for method:* a 2026-08-30 count over the 25 records the sweep showed put it at 3 of 25 (12%), and named its own limit — the 25 are the unfiltered head ordered by id, not a random sample. The head turned out to represent the tail.

**Must-not-suppress class — at least 197 of 534 (36.9%).** Records carrying at least one argument-bearing retry. This is a **floor, not a measurement**: it counts records where the detector fired, and the `60eba32c` finding above shows a semantic repeat with a varying argument is invisible to it. The direction of that error is the safe one for this candidate — an invisible repeat never becomes a `retry` event, so a rule scoped to the no-argument condition cannot take one with it. It is not safe for any future work that reads the retry class as a measure of how often the agent repeats itself.

**What suppression would actually buy, and it is not queue length.** 534 records become ~458. The queue is not long because of self-manufactured records. What suppression buys is signal-to-noise *within* a record: a reader stops seeing two lines of the loop obeying itself under every real finding. That is a real improvement and a much smaller one than "the backlog grows fastest when nothing is wrong" implies.

**Why the queue is actually long — a third mechanism, distinct from this node's subject.** A 2026-08-30 section concluded that 88% of records carry genuine `tool_error`s "that no Opportunity has ever been distilled from… That is discovery debt." That reading was wrong, and a 2026-08-31 pass corrected it by opening 7 records in full (`134bad7e`, `1329bda4`, `1eed544a`, `10002eba`, `0e0cd6f3`, `132bb394`, `6e1984b2`) and tracing all 26 `tool_error` events to nodes: **22 of 26 (85%) carry a signature the tree has already mapped and counted** — `File has not been read yet` alone is 13 of 26, censused corpus-wide at 416 events across 221 sessions on "The file changed after I read it, and the failed edit is how I find out"; plus `No such file or directory` (63 corpus-wide), `InputValidationError` on Read (46), Monitor's multi-operation approval refusal (8). Four were singletons this pass could not trace. **New needs distilled from all 7: zero.** So the queue is mostly *records whose signature is already mapped, which are never retired once mapped* — a mechanism distinct from both the noise this node is about and the debt that section inferred.

**Forward checks, not just retrospective ones — sixteen consecutive firings, every one at zero new needs.** Each firing scores its own arrivals before doing anything else. Recorded as a tally rather than as one dated section per firing, because the answer has been identical every time and eleven such sections had accreted across two consolidations before this was folded back in:

- 2026-08-31 — 3 arrivals, 12 events. Zero new needs.
- 2026-09-03 — 2 arrivals, 9 events (`62543b0f`: three `await` expiries, two read-before-write errors, one Edit old_string mismatch, one prescribed retry; `ea237dc4`: a `Glob` permission denial on the product repo plus the prescribed retry). Zero.
- 2026-09-07 — 1 arrival, 2 events (`7c2eebfc`, pure instance). Zero.
- 2026-09-08 — 1 arrival, 4 events (`1425fc57`: the prescribed pair, plus `Grep` refused on `/Users/tanner/ost-agent-meta/src` — right tool, wrong repo — and `Glob` refused on `/Users/tanner` for want of a read grant; both errors are the already-mapped Grep-wrong-repo-then-grant-denial signature `TRANSCRIPT:10fdeabc` carries, censused across eight records spanning 2026-08-06 to 2026-09-07 on "The agent has to guess what resources it's actually working with"). Zero.
- 2026-09-08 (later) — 1 arrival, 4 events (`4c80a477`: the prescribed pair plus two identical `Glob` denials on the product repo). Zero.
- 2026-09-08 (third) — 1 arrival, 3 events (`8e77fa17`, pure instance; the per-iteration yield finding above came from it). Zero.
- 2026-09-08 (fourth) — 1 arrival, 2 events (`6627cbf8`: one `Glob` denial on the product repo, one lone `ost_ingest_inbox` retry). Zero.
- 2026-09-08 (fifth) — 1 arrival, 1 event (`231e0b7a`, pure instance; the floor case above came from it). Zero.
- 2026-09-09 — 1 arrival, 4 events (`c6d01a0d`: the prescribed pair, plus `Grep` refused because ripgrep rejected a look-around pattern, and `Bash` reported as not available for the session). Both errors already-mapped: the regex-dialect refusal is held by "Name the engine's published dialect in the tool description and build no validator of our own", and the absent-tool discovery by "A run never states which tools it had, so a degraded pass reads like a full one afterwards". Zero.
- 2026-09-09 (later) — 1 arrival, 2 events (`8a4ea4e2`, harvested 18:30Z: a `Grep` permission denial on `/Users/tanner/dev/OST-Agent/src` plus one prescribed `ost_ingest_inbox` retry). Both already-mapped classes. Zero.
- 2026-09-09 (fifth) — 1 arrival, 1 event (`99190d4a`, harvested 23:00Z: a single `retry` on `ost_ingest_inbox` `{}` with zero `tool_error` — the second pure-instance floor case). Zero.
- 2026-09-09 (sixth) — 1 arrival, 2 events (`073a51de`: a `Glob` `InputValidationError` naming an unexpected `head_limit` parameter, plus one prescribed retry; the parameter-refusal is the already-mapped `InputValidationError` signature). Zero.
- 2026-09-10 — 1 arrival, 2 events (`9a31e703`: a `Glob` permission denial on `/Users/tanner/dev/OST-Agent` plus one prescribed retry). Both already-mapped. Zero.
- 2026-09-11 — 1 arrival, 1 event (`fe37c31f`, harvested 23:07Z: a single `retry` on `ost_ingest_inbox` `{}` with zero `tool_error` — the third pure-instance floor case). Zero.

The mix is what the standing census predicts: pure instances and already-mapped signatures, and no arrival in sixteen consecutive checks has carried a need the tree lacks. The queue grows from records whose signature is mapped and never retired, not from unmet needs waiting to be distilled.

**One class of arrival is avoidable, and six firings of self-observation established what it is.** Folded here from the dated sections that produced it, because the arms are only readable together.

*The class.* The `Glob`/`Grep` denial on `/Users/tanner/dev/OST-Agent` regenerates roughly once per firing — `79b70e58`, `ea237dc4`, `4c80a477`, `6627cbf8`, `8a4ea4e2`, `9a31e703` and more besides. It is not a missing capability: `ost_read_repo` reads that repository fine and has been used on many passes to list `test/` and read source files whole. What `ost_read_repo` cannot do is *search*, which is a real gap. So a firing reaching for `Glob`/`Grep` to **list or read** the product repo is burning a call and minting an unretirable record for something it already has; a firing reaching for it to **search** is hitting a genuine limit. Both produce the same friction line, so the corpus cannot tell an avoidable denial from a blocking one — and only the second kind is evidence of anything. Any manifest built here would suppress the prescribed retries and leave this pair untouched, because a permission denial is a genuine `tool_error` and must not be suppressed.

*The corrections-ledger route, and why it is more expensive than it first looked.* What would retire the avoidable class is naming `ost_read_repo` as the route to the product repo at the point where a firing is told the repo exists. A mechanism that already does that job ships and is populated on this workspace: the unattended firing prompt opens with a **CORRECTIONS ALREADY ISSUED IN THIS WORKSPACE** block, a persisted ledger of calls the surface has already refused here, each with the permitted form and a recurrence count across sessions. It holds two entries — the `sleep`-chaining block and a `Bash`-not-available error — and across at least six consecutive readings (2026-09-08 through 2026-09-11) it has never gained the product-repo denial class, while the class minted several further records in the same window. The `sleep` entry is the control: unchanged at 10 times across 10 sessions, it demonstrates the mechanism does accumulate a recurrence count when it records a class at all, so the absence is not staleness in the ledger generally. The inference is therefore that a *permission-not-yet-granted* prompt is a kind the ledger does not record by construction, and the repair is a change to its inclusion rule rather than one line added to a list — more expensive than the first estimate, standing in direction and stripped of its cheapness. *Limit:* this is an inference from an absence. Nothing here reads the ledger's implementation, which is not in `product.repos` and is not reachable from this surface, and a mechanism that records a class only above some higher threshold would look identical.

*The compose-first / read-first experiment, both arms and the refutation.* Two readings were available: a genuine capability gap, or a warning-placement defect. Four firings tried to separate them. **Compose-first, n=2:** the 2026-09-09 (fifth) and (sixth) firings each reached for `Glob` on the product repo as an early call, *before* opening this node or any other node in the tree, were denied, and then reached the same repository through `ost_read_repo` on the next call without difficulty. **Read-first, n=1:** the 2026-09-09 (later) firing had read this node and minted no denial — explicitly discounted at the time as not independent. That split argued for warning-placement: if the capability were genuinely missing it would recur whatever the firing had read, and it did not. **The refuting arm, 2026-09-10:** a firing that had *not* read this node — it had opened exactly one node, the under-served opportunity carrying its own standing hold — reached the product repo through `ost_read_repo` on its first attempt and minted no denial. On its own terms the compose-first/read-first split does not survive that.

*Where the refutation left the finding, and what 2026-09-11 does to it.* The 2026-09-10 pass rescued the placement reading by relocating it: its own prompt names the tool outright in the outward-sensing paragraph — "`ost_read_repo` for the product repository this tree is about" — so something *did* tell it, before it composed, that the repo is reachable under another name. It also noted the cost of that rescue: if the paragraph is in every firing's prompt, the warning was present for all the prior denials too, placement does not separate the arms, and the split is unexplained. **The 2026-09-11 firing settles that against the paragraph.** It read `ost.config.yaml` to locate the repo, composed `Glob` against `/Users/tanner/dev/OST-Agent/test` as its very next call — having opened no node in the tree — and was denied; it then listed the same directory through `ost_read_repo` without difficulty. Its prompt carried the outward-sensing paragraph verbatim. So the paragraph's presence is *not* what averts the denial: one firing read it and composed the `Glob` anyway. The placement reading, as rescued, is refuted, and the compose-first arm stands at n=3.

*What survives, and it is the reading the 2026-09-10 pass proposed as sharper.* The arms differ in what the firing was **trying to do**, not in what it had read. The 2026-09-10 firing wanted to read named files and did so through `ost_read_repo`; when it wanted to *search* it did not compose a `Grep` at all, leaving `src/eval/buildable.ts` unread past its truncation point and running `Grep` over the vault instead. The 2026-09-11 firing wanted to *enumerate* the spec suite — which `ost_read_repo` lists but cannot glob or grep — and reached for the tool that does that. A firing that needs a named file will not mint the denial whatever it has read; a firing that needs to search or enumerate will reach for the search tool it knows, because the capability it wants genuinely does not exist on this surface. That makes the class a **capability gap after all, narrowed to search**, rather than a warning-placement defect — the opposite of where three firings of self-observation had it, and the repair is correspondingly different: give `ost_read_repo` a search or glob mode, or grant the product repo to `Grep`. Neither is a manifest, and neither is a line in a ledger.

*Why this is recorded here rather than as a new candidate, and it costs this node.* It sharpens this node's scope against it. This node's subject is suppressing procedure *after* it is harvested; every repair above acts *before* the call is made, so none of them suppresses the record — they prevent the call that mints it. Different mechanisms, different failure modes, and theirs is the better one for this class, precisely because a permission denial is a genuine `tool_error` that no manifest may suppress.

*This line of self-observation should now stop.* Both arms are this product's own agent observing itself, which is the least independent evidence available, and no number of further instances escapes that dependence. Composition order was never randomised: every firing reached for `Glob` first because it was locating or enumerating the product repo, so the arms differ in what had been read *and* in what was wanted, and only the second turned out to matter. The finding is actionable as it stands. Future passes: record the arrival in the tally above and leave this line alone. Two observations would change it and are worth making — the ledger gaining the class, or a firing that wants to *search* the product repo and gets there without a denial. The second would mean the capability landed.

**Method and limits, for the whole census.** Counts are per-record, not per-event, so nothing above describes the event mix inside the 197. The retry-only set was identified from header parentheticals rather than by parsing each event list, so a malformed header would be miscounted. The claim that no-argument retries are only ever `ost_ingest_inbox`/`ost_next_work` rests on a 40-line sample. The signature-to-node tracing was done by reading node bodies rather than by grep, so a mapped signature could have been missed and counted as a singleton — an error that runs towards *understating* how much of the queue is already mapped. 534 is every transcript record on disk and is a larger denominator than the unmapped count the sweep reports; the two answer different questions and should not be differenced. On the forward checks specifically: one arrival is one arrival and not a rate, so no entry in that tally re-measures the 14.2% or 85% figures; the per-iteration call counts are inferred from retry counts plus the second-appearance rule read off `src/adapters/transcript.ts`, not from observing the calls, and a change to how retries are emitted would void the inference; the attribution of an extra ingest to a second sweep iteration is read off the loop's prompt text rather than the session transcript, which this surface cannot open. On the avoidable-denial analysis specifically: n=3 compose-first and n=2 read-first, all of them this product's own agent, and the 2026-09-11 observation that refutes the paragraph rescue is a single firing's own behaviour — the least independent evidence there is, and it cannot be separated from that firing having gone on to write this paragraph. The want-to-search reading is consistent with all five observations and was not tested against a firing that wanted only named files and had read nothing, which is the arm still missing. Nothing above was executed: no test was run, no rung moved, no instrument set, no status changed. This is observed behaviour of this product's own agent — it grounds usability, not desirability, and is not evidence that anyone wants anything.

**None of it touches this candidate's sharpest risk**, which is whether the founder keeps the manifest current. That belief is untouched by every count above and stays where the parent assumption has it.

## Issues
- 2026-08-30 2026-08-30 unattended sweep — the mechanism behind this node's own lane note, read off the source rather than inferred. This node records that its test "was created in the humans-required lane at creation time" and that `ost_flag_humans_required` is withheld on the unattended surface. What it did not know is why that combination makes the node reappear in `solutionsMissingInstruments` on every pass regardless: that function in `src/eval/buildable.ts` applies no lane check at all, while `testsAwaitingVerification` two functions below it in the same file does (`if (n.lane === CAUTIOUS_LANE) continue;`). This node's test was read directly this pass and does carry `lane: humans-required` in frontmatter — the label is present and the bucket ignores it. So the census entry is not a judgement about this node; it is a queue that cannot express the decision this node already made. Distilled into a new Opportunity beneath "The pass never says it is done, so I can't tell when to stop paying for compute" rather than argued here, and named in plain text so this node keeps its single backlink: "Work I already decided needs a person comes back every pass as work I failed to do". Nothing else about this node changed — its humans-required Definition of done, its census and its qualifications all stand unaltered, and no instrument was set.

## History
- 2026-09-03 body edited, dropping `## First-party instance: a friction-free firing that manufactured its own friction record (2026-08-28)`, `## The prediction was checked and held (2026-08-28 sweep)`, `## 2026-08-29 — the cheap mitigation does not cover this node's failure case`, `## 2026-08-30 — the census this node has called open three times, counted over a 25-record sample`, `## 2026-08-30 — one qualification to the count above, from a fifth body read`, `## 2026-08-31 — the 88% figure re-measured by reading records instead of counting tallies, and it does not mean what the section above took it to mean`, `## 2026-08-31 — the errand left open above is closed (pointer only)`, `## 2026-08-31 — the census, measured over all 534 records instead of a head of 25`, `## 2026-08-31 — the generator observed across two consecutive firings, and a third class that is avoidable (later firing)`, `## 2026-09-02 (later firing) — the repeat detector has a blind spot in the opposite direction, and the 197-record figure inherits it` — Eleven dated sections had accreted here, growing the node to 33,576 characters while nine of them opened with some version of "kept short, per this node's convention". Three of them supersede each other on the same number (a 12% head-of-25 estimate, then a 14.2% full-corpus count; an 88% figure and the later section that corrects what it means; an age-out caveat and the later section that discharges it), so a reader arriving today has to read all eleven in order to learn which figures still stand. Folding them into one standing finding and one standing census. No distinct claim, number, source citation or limit is dropped — every one is reproduced below, with the superseded estimates marked as superseded rather than deleted, and git holds the full prior text. Also adding this firing's own forward check (2 arrivals, 12 friction events, zero new needs), inside the census rather than as a twelfth dated section. This is the same repair a 2026-08-21 pass applied to "Append-only tool surface with no delete or shell tool", for the same reason: a node whose ask is buried under ten restatements of itself is a node nobody reads to the end of. Nothing about the candidate's claim, its humans-required Definition of done, or its Issues note changed.
- 2026-09-09 body edited, dropping `## 2026-09-07 — fifth consecutive forward check, and this one is a pure instance`, `## 2026-09-08 — sixth consecutive forward check, and this arrival is the already-mapped kind rather than a pure instance`, `## 2026-09-08 (later firing) — seventh consecutive forward check, and the reading firing committed the avoidable denial itself`, `## 2026-09-08 (third firing) — eighth consecutive forward check, and the first instance that shows the generator's yield is per-iteration, not per-firing`, `## 2026-09-08 (fourth firing) — ninth consecutive forward check, and the open errand about the avoidable class has a shipped answer nobody has used`, `## 2026-09-08 (fifth firing) — tenth consecutive forward check, a pure instance at the smallest size the generator can produce` — Six dated forward-check sections had re-accreted since the 2026-09-03 consolidation (2026-09-07, and four separate firings on 2026-09-08), growing the node back to 33,740 characters, with five of the six opening with some version of "four lines, per this node's convention". The convention is self-defeating at this length: a reader arriving today must read six dated sections in order to learn that the answer in all of them is the same — zero new needs — and to find the two that carry a genuinely new mechanical finding (the per-iteration yield, and the corrections-ledger route). Folding all six into the standing census, which is where the counts they report already belong. No distinct claim, number, record id, design consequence or limit is dropped: the ten-firing tally, the ten-name pure-instance list, the calls-minus-one yield rule and its count-independent design consequence, the single-retry floor case, the self-committed avoidable denial, and the corrections-ledger finding with its unknown-inclusion-rule caveat are all reproduced below. Git holds the full prior text. This is the same repair the 2026-09-03 pass applied to this node and a 2026-08-21 pass applied to "Append-only tool surface with no delete or shell tool", for the same stated reason. This firing's own forward check (1 arrival, 4 events, zero new needs) is recorded inside the census as a line in the tally rather than as a seventh dated section — deliberately, since adding one would be the exact pathology this edit repairs. Nothing about the candidate's claim, its humans-required Definition of done, its standing finding or its Issues note changed.
- 2026-09-11 body edited, dropping `## 2026-09-09 — the corrections-ledger limit is discharged, and the answer is the expensive one`, `## 2026-09-09 (later firing) — twelfth forward check, and the fourth avoidable-denial instance is now harvested`, `## 2026-09-09 (fifth firing) — thirteenth forward check, and the avoidable-denial claim finally gets its control arm`, `## 2026-09-09 (sixth firing) — the compose-first arm replicates, and this line of self-observation should now stop`, `## 2026-09-10 — the refuting arm the section above asked for, supplied; it holds, and it narrows what the finding proves` — Five dated sections had re-accreted since the 2026-09-09 consolidation (one on 2026-09-09 discharging the corrections-ledger limit, three more 2026-09-09 forward checks, and the 2026-09-10 refuting arm), growing the node back to 42,319 characters — larger than either state the two prior consolidations were applied to. Four of the five open with some version of "four lines, per this node's convention", which is the convention defeating itself: a reader arriving today must read five dated sections in order to learn that the forward-check answer in all of them is the same (zero new needs) and to assemble the avoidable-denial experiment, whose two arms and their refutation are spread across four of them and readable in order only. Folding all five into the standing census, where the counts and the class analysis already belong. No distinct claim, record id, arm, control reading or limit is dropped: the twelfth through fifteenth forward checks, the second pure-instance floor case (99190d4a), the corrections-ledger inference-from-absence with its unknown-inclusion-rule caveat and its sleep-entry control, the compose-first arm at n=2, the refuting arm and its relocation of the finding to the prompt's outward-sensing paragraph, and the want-to-search reading are all reproduced below. Git holds the full prior text. This is the same repair the 2026-09-03 and 2026-09-09 passes applied to this node and a 2026-08-21 pass applied to "Append-only tool surface with no delete or shell tool", for the same stated reason. This firing's own forward check (1 arrival, 1 event, zero new needs) and its own compose-first observation are recorded inside the census rather than as a sixth dated section — deliberately, since adding one would be the exact pathology this edit repairs. The candidate's claim, its humans-required Definition of done, its standing finding and its Issues note are unchanged.
