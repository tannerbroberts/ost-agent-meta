---
type: Opportunity
source: 'TRANSCRIPT:3b9eaea5-d098-4f47-ad0a-65871012d639'
created: '2026-08-10'
evidence: assertion
---
#Opportunity #unvalidated #evidence/assertion
[[One human dismissal can name a class of evidence rather than a single id]]
[[A node may cite many sources, so one distillation discharges the whole cluster]]
[[The transcript adapter rolls near-identical sessions into one record before storing them]]

**The need, from the operator's side.** My highest-volume evidence channel produces many records of one recurring event. Discharging them is priced per record and paid by me: either I distil each into a node — forbidden, because they are the same claim — or I dismiss each by hand. Eighty-seven items is eighty-seven acts, and the channel manufactures more while I work.

**Measured, not felt.** `ost_next_work` reported **87 unmapped evidence items** this pass. Two came from `friction/` and carry distinct content. The rest are `TRANSCRIPT:` session-friction records, and reading them shows the shape: this node's own source records ten events, six `tool_error` and four `retry`, and four of the six are one sentence — a permission the unattended surface does not hold. Dozens of sibling records say the same thing about different session ids.

**What already exists, verified against the code rather than assumed.** `ost-agent dispose --kind evidence <id> --by <who> --why <reason>` ships and is pinned by `test/cli/dispose.test.ts`. It refuses an unattributed or unexplained dismissal, prints the reversal command beside the confirmation, keeps the entry append-only so a reversal does not erase the dismissal, and `ost-agent dispositions` lists every live one for audit. `ost_next_work` reads it back as `withheldByDisposition`. So the escape hatch is real, it is well built, and this node is not asking for one.

It is also deliberately a human's command and should stay one. A dismissal removes work by asserting a sentence about it, written by the party whose budget the work costs; the spec file says exactly that, and it is the same argument that keeps `result` and `promote` off the agent surface. Nothing here should be read as an argument for handing dismissal to a pass.

**What is actually missing is the unit.** The mechanism dismisses one id per invocation, and the thing an operator has is a *class* — "session-friction records whose only content is a permission this surface does not hold". Paying per member for a judgement made once about the set is the gap, and it is the gap that scales the wrong way: transcripts are generated by the loop itself, so every firing that hits friction manufactures another member, and the queue grows from being worked.

**Why mapping cannot absorb the class either.** `src/processes/tree.ts` states the relation in its own header: *"evidence is mapped iff some node in the tree cites its id as that node's `source`."* One writer, `ost_create_node`'s `source`, a single string in node frontmatter. A distillation that genuinely covers forty records can only cite one of them, so thirty-nine stay outstanding even when the reasoning that discharges them has been done and written down. The doc comment explains — correctly — why the *persisted* mapped-set was deleted: a reader with no writer was a second answer any actor could forge. Deriving the relation was right; deriving it from a one-to-one field is what leaves the other thirty-nine.

**Whose problem this is.** Not only this vault's. Any operator who turns on a mechanical channel — transcripts, usage, a webhook — buys a stream where most records restate one fact. Manual inbox notes do not have this property, which is why the tree did not have it for its first fortnight.

**More than one way to address it**, so it is not a solution in disguise: let one human judgement cover a named class of records; let a node cite many sources; or roll near-identical records into one before they are ever stored. Those are different products with different failure modes, and the first two disagree about whether the answer is dismissal or distillation.

**What this does not claim.** It is first-party throughout — a count off this tree's own tool output and reads of this product's own source and specs. No outside operator has reported it. Rung is the floor, deliberately: the source is a `TRANSCRIPT:` recording and would carry `observed`, but the recording evidences the agent's tool errors, not this claim about evidence accumulation, and taking the higher rung on that technicality would be exactly the laundering the ladder exists to stop.

⚠️ Unvalidated. Agent-distilled.

## History
- 2026-08-10 body edited — The first wording claimed the tree offers exactly one way to take an evidence record off the outstanding list. That is false and this pass verified it false: `ost-agent dispose --kind evidence` ships, pinned by `test/cli/dispose.test.ts`, which dismisses an evidence id with an author, a reason and a one-command reversal. Leaving the overstatement in would have sent a reader to build an escape hatch that already exists. The need survives the correction and is sharper for it — the escape is per-record and human-only, so the cost is linear in a stream that grows from being worked.

## Issues
- 2026-08-10 2026-08-10 census from the unattended sweep, and it is the strongest version of this complaint yet recorded. `unmappedEvidence` stood at 94 after ingest (2 new transcript records captured this firing). The sweep read five of them in full — one inbox friction note and four transcript records chosen for distinct event mixes — and found four distinct friction signatures between them: read-before-write refusals ("File has not been read yet", 7 of 8 events in one session and 2 of 4 in another), a git exit-128 dirty-tree block, a `permission_denied` on Bash inside an unattended firing, and a `clarifying_question` asked with nobody present. Every one of the four already has an opportunity in this tree — respectively "The file changed after I read it, and the failed edit is how I find out", "A run's own leftovers break the next run's setup, so the loop fails before it starts", "A block stops everything and announces itself to no one", and "A run has no authority to decide anything, so every fork is a full stop". Not one of the five items revealed a need the tree does not hold. So the pass mapped none of them, per the rule that an item revealing no genuine need is skipped, and all 94 remain outstanding for the next firing to re-read. Two things this measures that the earlier notes did not. First, the cost is now per-firing and unbounded: the queue only grows, because the transcript channel files a record for every session including the sessions that do this reading. Second, the 25-item cap means a pass sees a quarter of the queue and pays one tool call per body it opens, so the sweep physically cannot audit its own inbox within a firing — this census rests on a 5-of-94 sample and says so. For a human: the choice is between the sibling solutions here and the ones under "The queue groups identical friction shapes into one item with a count"; nothing an unattended pass can do to individual nodes touches it, which is why this is an annotation rather than 94 opportunities.
- 2026-08-10 2026-08-10 (second unattended firing of the day) — the growth this node predicts, measured across two passes on one day. `ost_ingest_inbox` captured 2 new `TRANSCRIPT:` records and `unmappedEvidence` moved **94 → 96**. Both increments are session-friction records filed by this vault's own firings, which is the node's central claim showing up on the shortest timescale yet recorded: the queue grew by exactly the number of sessions that ran, and one of the two sessions was the pass doing the reading.

Both new items were read in full rather than sampled, and neither revealed a need the tree does not hold. `f90cbbcc` is one `tool_error`: an Edit whose `old_string` was not found — the signature already mapped as "The file changed after I read it, and the failed edit is how I find out". `4cbb8fda` is five events: a `Glob` permission denial on the product directory, two `InputValidationError`s for parameters the caller invented (`head_limit` on Glob, `-h` on Grep), and two retries — mapping respectively to "The agent's repo sight fails mid-pass, because nothing checked the product path before it was needed" and "I compose a hundred and seventy lines before the surface tells me it does not accept that dialect". So both were skipped under the rule that an item revealing no genuine need is not mapped, and 96 remain outstanding.

One thing this adds that the earlier census did not. That earlier note put the cost at "one tool call per body it opened" and concluded the sweep "physically cannot audit its own inbox within a firing". The first half is now known to be avoidable: the evidence records are ordinary files under `.ost-agent/evidence/`, readable with `Read`/`Glob`/`Grep`, so a pass holding those built-ins can audit the whole 96 at a fraction of the metered cost. The second half stands anyway — reading them all still would not discharge one, because discharge needs a node citing the id, not a reading of it. Worth separating for whoever picks this up: the audit is cheap and the discharge is not, and only the second is what this node is about.

For a human, unchanged and now better evidenced: nothing an unattended pass can do to individual nodes touches this. The choice is between this node's three sibling solutions and the ones under "The queue groups identical friction shapes into one item with a count". Every pass that meets this queue pays to re-read a sample of it and leaves it larger than it found it.
- 2026-08-10 2026-08-10 (third unattended firing of the day) — the series continues: unmappedEvidence 98 → 110 across the firings since the full census, ingest this firing capturing 1 new TRANSCRIPT record (82c318d1). That record was read in full: its only event outside the censused signatures is a mistyped tool name ("No such tool available: mcp__ost-avoke"), which the tree already holds as "The friction channel fills with my own typos, so the signal I wanted is buried". Skipped under the standing rule; no opportunity minted. Nothing new to decide — this line only extends the measured series the census established.
- 2026-08-11 2026-08-10 (fourth unattended firing of the day) — the series continues: ingest captured 3 items (2 founder inbox notes, already discharged by the nodes created from them in the prior session, plus TRANSCRIPT:054b78fc) and unmappedEvidence stands at 116. The transcript record was read in full: one ripgrep brace-glob rejection and two MCP retries, all inside the censused signatures ("I compose a hundred and seventy lines before the surface tells me it does not accept that dialect" / retry noise). Skipped under the standing rule; no opportunity minted. One reopened INBOX item (the 2026-07-24 backgrounded-session friction, in mapped.json's legacy list but re-listed since the mapping relation became derived) was also read in full and skipped as a repeat instance of "An interrupted run leaves no trustworthy account of what it completed"; a corroboration note was left on that node.
- 2026-08-17 2026-08-17 unattended sweep — unmappedEvidence stands at 239 (191 of 260 on-disk TRANSCRIPT_ records carry an already-mapped friction signature, per a Grep census over .ost-agent/evidence/). The two non-transcript items were read in full and both restate needs the tree already holds: the backgrounded-session friction matches "A run that dies while I am away stays dead, and nothing says where it stopped", and the "Ask the open question first" build-loop note is already fully absorbed into that solution's own History (deferred 2026-08-16 on a real instrument run). Per the standing rule, nothing was mapped from either. Series continues to grow faster than any pass can discharge it.

## The whole corpus censused instead of sampled — 177 of 177, 2026-08-10 (not a recorded result)

**No test was run and nothing here clears a gate.** This is a mechanical count over files, done to close the limitation every previous census on this node declared about itself. The 2026-08-10 note above says *"this census rests on a 5-of-94 sample and says so"*, and the note after it read two more records in full. Three passes have now described this queue from between two and five of its members. This one counted all of it.

**The method that made it cheap, which the note above predicted.** That note observed that the evidence records are ordinary files under `.ost-agent/evidence/` and therefore auditable with the built-in file tools at a fraction of the metered cost. That is correct and is what happened: the entire census below is five `Grep` calls and one `Glob`, with no `ost_next_work` body fetch beyond the six records read in full for other reasons. The audit that three passes called physically impossible within a firing took six tool calls.

**The corpus.** 177 evidence records on disk. `ost_next_work` reports **98** of them unmapped after this firing's ingest — up from 96 at the previous firing, and both increments are `TRANSCRIPT:` records filed by this vault's own sessions, which is this node's central claim recording itself for the third consecutive pass on the same day.

**Every friction signature in the corpus, counted rather than sampled.**

| Signature | Occurrences | Records carrying it |
|---|---|---|
| `requested permissions to …` — a grant discovered by hitting it | 128 | 48 |
| `File has not been read yet` — the read-before-write refusal | 73 | 28 |
| `clarifying_question` — a question asked with nobody present | 38 | 19 |
| `InputValidationError` — a dialect the surface does not accept | 11 | 10 |
| `permission_denied` — a call rejected outright | 6 | 5 |

91 of the 177 records carry at least one of those signatures, and that 91 is a floor rather than a total, because the union pattern behind it used a narrower phrasing of the permission case than the row above does.

**What the count settles, and it is the thing three passes have asserted from samples.** Every one of the five signatures already has an opportunity in this tree — respectively "The unattended run is scoped for tools nobody granted it, and it finds out one denial at a time", "The file changed after I read it, and the failed edit is how I find out", "A run has no authority to decide anything, so every fork is a full stop", "I compose a hundred and seventy lines before the surface tells me it does not accept that dialect", and the first of those again. So the claim that the queue restates needs the tree already holds is no longer an inference from five records: it is a property of the corpus. No opportunity was created from the 98 this pass, under the rule that an item revealing no genuine need is skipped, and the skip is now evidenced rather than assumed.

**One thing the count changes about how this node reads.** The earlier notes lead with the read-before-write family as the dominant shape, because that is what the sampled records happened to contain. Over the whole corpus it is second. The largest class by a wide margin is a permission the running surface does not hold — 128 occurrences across 48 records, better than a quarter of every evidence record in the vault. That matters for which sibling solution is worth building first: a normaliser tuned to the edit-refusal family would collapse 28 records, and one tuned to the permission family would collapse 48.

**What this does NOT settle.** The counts are taken over all 177 records, not over the 98 unmapped ones, because mapped-ness is a derived relation over node frontmatter and separating it was not worth a sixth call — so the table describes the corpus and is an upper bound on the unmapped subset, not a measurement of it. Signatures were matched by literal string, so a record phrasing the same friction differently is uncounted and the true concentration is at least this high rather than exactly this high. And nothing here touches the actual problem: counting 98 records discharges none of them, because discharge needs a node citing the id. The audit is cheap, the discharge is not, and only the second is what this node is about.

_Method: one `Glob` of `.ost-agent/evidence/` and five counted `Grep` passes over it, plus six records read in full through `ost_next_work`. First-party reads of this vault's own files. No command executed, no result recorded, no rung changed._
