---
type: AssumptionTest
created: '2026-08-18'
evidence: assertion
lane: humans-required
threshold: >-
  All write paths confirmed able to carry a session id with no breaking
  consumer, or a specific gap named
---
#AssumptionTest #unvalidated #evidence/assertion

Small, fast check: read the commit-producing code paths to confirm a session id is available at every write site, and check for existing tooling that parses commit-message format strictly enough that appending a tag would break it. Threshold: all paths can carry it cleanly, or a specific path/consumer can't.

A person outside the building is the measurement here: This pass holds no repo-read grant to inspect commit-producing code paths or the tooling that parses commit messages. A person with the codebase open (or an attended pass with ost_read_repo) can check this directly.

## Issues
- 2026-08-21 2026-08-21 (unattended sweep) — the stated reason for the humans-required label no longer holds, and the repository answers the first half of this. The node's own last line gives the reason a person is the measurement: "This pass holds no repo-read grant to inspect commit-producing code paths or the tooling that parses commit messages." This pass held `ost_read_repo` and read them. FINDING on the first half of the threshold ("all write paths confirmed able to carry a session id"): every product commit passes through ONE function, so this needs no per-call-site audit. `src/git/safe-git.ts` exposes exactly three git operations and its `gitCommit(dir, message)` docstring names itself the funnel — "this is the funnel every commit the product makes passes through — the MCP auto-commit, the `git_commit` tool, `init`, `set-outcome`, the friction CLI." A session id therefore reaches every write path through that single `message` parameter. The module is deliberately closed (no reset, rm, clean, branch delete, history rewrite or force flag anywhere), which is what makes the single-funnel claim credible rather than merely documented. SECOND HALF NOT SETTLED, and this is the part still owed to somebody: "without breaking commit-message parsers" needs the set of consumers that parse commit messages strictly. This pass did not enumerate them. What it can say is that one consumer of message format is already specified and would be the thing to re-run: `test/git/commit-provenance.test.ts` asserts three properties of the message — it contains the node title, it contains the source id, and it omits a fabricated "(source:" marker when a node has no source — so any session-id tag must leave those three intact. The one release-side parser checked, `src/release/next-version.ts`, reads registry responses and `git ls-remote` tags and does not parse commit messages at all. WHAT THE SPEC WOULD ASSERT, if a human re-lanes this: create a node, append to it, and run init/set-outcome against one vault; require each resulting commit message to carry the run's session id; then require the three existing commit-provenance assertions to still pass. NOT ACTING ON THIS: `ost_set_instrument` refused, correctly — "it is labelled humans-required, so a person is the measurement and no command can stand in for them... Removing a caution is not a call this surface makes." A human who agrees can re-lane with: ost-agent lane "Have someone with the vault-write code open confirm every commit path can carry a session id without breaking commit-message parsers" --set compute-only. Source: this pass's `ost_read_repo` reads of src/git/safe-git.ts, test/git/commit-provenance.test.ts and src/release/next-version.ts. First-party read of committed code; grounds feasibility only. No command executed, no result recorded, rung unchanged.
