---
type: Assumption
source: 'agent-ideation:2026-09-01-unattended-sweep'
created: '2026-09-01'
evidence: assertion
authorship: machine
---
#Assumption #unvalidated #evidence/assertion
[[The unblocking score separates the queue instead of collapsing to one value]]

**Category: feasibility.** Stated so it can be false: *the tests on the ask queue differ enough in what they unblock — how many solutions depend on them, whether they are the sole test under their assumption, what declares them a prerequisite — that a leverage score separates them.*

The parent opportunity records the pattern this is exposed to, in the words of the node above it: *"an attribute that is constant across the corpus cannot contribute to any ordering built on it."* That has now been observed three times on this tree — status is universally `unvalidated`, authorship is 0 of 1606 human-written, and age is null for 62 of 62. A fourth instance is a live possibility rather than a hypothetical, and this candidate is the one that would produce it: if most queued tests are the only test under their assumption and almost nothing declares a prerequisite, the score is near-constant and the sort is flat again.

What makes this worth testing rather than assuming: the input is already fully present. Unlike age, no field needs populating first, so the distribution can be computed against today's tree before anything is built.
