---
type: AssumptionTest
created: '2026-08-18'
evidence: assertion
lane: humans-required
threshold: >-
  Build script confirmed deterministic and network/environment-independent, or
  confirmed to have a specific non-deterministic step
---
#AssumptionTest #unvalidated #evidence/assertion

Small, fast check: read the build script that produces dist/ for non-deterministic inputs (timestamps, network fetches, environment-dependent output) that would make it unsafe to re-run as a merge driver. Threshold: the build is confirmed reproducible from source alone, or it isn't.

A person outside the building is the measurement here: This pass holds no repo-read grant to inspect the build script for non-determinism. A person with the codebase open (or an attended pass with ost_read_repo) can check this directly against the actual build config.

## Issues
- 2026-08-21 2026-08-21 (unattended sweep) — the stated reason for the humans-required label no longer holds, and the repository substantially answers this. The node's own last line gives the reason a person is the measurement: "This pass holds no repo-read grant to inspect the build script for non-determinism." This pass held `ost_read_repo` and inspected it. FINDING, against the threshold as written ("the build is confirmed reproducible from source alone, or it isn't"): the build command carries no non-deterministic input. `package.json` defines the artifact as `bundle`: `esbuild src/cli/index.ts --bundle --platform=node --target=node20 --format=esm --alias:jsonc-parser=jsonc-parser/lib/esm/main.js --banner:js=<fixed literal> --outfile=dist/ost-agent.mjs`. No timestamp, no network fetch, no environment interpolation, no codegen step, single fixed entry point and a constant banner. Its inputs are the source tree and the resolved dependency set, nothing else. TWO RESIDUAL RISKS the read does surface, and they are the specific gap the threshold's second branch asks for rather than a clean pass: (1) esbuild is declared `^0.28.1`, a caret range, so two installs can resolve different bundler versions and produce different bytes — `package-lock.json` is committed, so this closes if the merge step installs with `npm ci` and stays open if it uses `npm install`; (2) the driver needs `node_modules` present and correct at merge time, which is not a determinism question but is exactly the sibling assumption beneath this same solution ("A build step is reliably available in the environment that resolves a git merge on an unattended firing") — the two are not independent and a human should read them together. Confirmed separately from `.gitignore`, which carries `dist/*` plus `!dist/ost-agent.mjs` and a comment explaining that the non-wholesale form is deliberate so the bundle can be committed: the premise of the parent solution, that dist is a checked-in artifact, holds. NOT ACTING ON THIS: no instrument set and no lane changed. `ost_set_instrument` refused the attempt on the sibling test of this same shape, correctly — "it is labelled humans-required, so a person is the measurement and no command can stand in for them" — and removing a caution is not this surface's call. A human who agrees the repository settles it can re-lane with: ost-agent lane "Have someone with the build scripts open confirm the dist build is deterministic enough to run inside a git merge step" --set compute-only. Source: this pass's `ost_read_repo` reads of package.json and .gitignore. First-party read of committed code; grounds feasibility only. No command executed, no result recorded, rung unchanged.
