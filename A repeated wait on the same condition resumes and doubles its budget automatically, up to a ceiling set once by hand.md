---
type: Solution
source: 'agent-ideation:2026-08-29-unattended-sweep'
created: '2026-08-29'
evidence: assertion
authorship: machine
---
#Solution #unvalidated #evidence/assertion
[[Conditions that expire are more often genuinely slow than never going to be true]]

**Variation dimension: automated-vs-manual. Position taken: the escalation is automated, the ceiling is deliberately manual.**

The wait keeps a small on-disk record keyed by the condition string. The first wait on a condition runs its default budget. A second wait on the *same* condition does not start over: it recognises the key, reports how long it has already spent in total, and doubles its budget — 300s, then 600s, then 1200s — until a total ceiling that a person sets once. When the ceiling is reached the wait refuses to run again and says so, naming the accumulated time.

What stays manual is the ceiling, because it is the only part that encodes a judgement nothing local can make: how much unattended wall clock this operator is willing to spend on one condition before the honest answer is that the approach is wrong.

**Why this shape addresses the observed failure directly.** The 2026-08-29 record on the parent shows five expiries and two byte-identical re-issues. Under this candidate the second call would have said "already spent 300s on this condition, extending to 600s" and the fifth would have refused outright. The information the session lacked — that it was repeating itself and how much it had already burned — is information the waiter already has and currently throws away between invocations.

**Against its siblings.** The heartbeat candidate makes one expiry informative; this one makes a *sequence* of expiries informative and needs no cooperation from the process being waited on, so it works on remote and queued subjects the heartbeat cannot reach. The operator-recorded-instruction candidate is cheaper and asks nobody to build state, but it cannot tell the caller that this is the fourth attempt.

**What would make this the wrong pick.** It rewards patience mechanically, and patience is often the wrong answer: a condition that will never become true gets 2100s of automatic indulgence instead of 300s, which is worse, not better, for an unattended run paying by the minute. It is the right pick only if genuinely-slow subjects are commoner than never-true ones — which nothing here has counted. It also inherits an identity problem: two different waits on the same condition string in the same session are indistinguishable from one wait retried, and collapsing them is sometimes wrong.

**Adjacent node a human should read beside this.** The tree already carries "A budget that is missing, empty or non-numeric makes the wait say so rather than quietly using 300" under a different parent. That is about a budget that never arrives; this is about a budget that arrives, is honoured, and is then discarded between calls. Related mechanism, different failure.

**No instrument.** The helper is on the session's PATH and supplied by the harness, not in this repository, so no spec in this product's `test/` can reach it.

Unvalidated. Agent-ideated on 2026-08-29; a human to review.

## Definition of done — and it is not a command

"Count the recorded expiries that later succeeded against those whose condition could never have become true"

There is deliberately no instrument. The bar is: at least 2 of every 3 expiries were genuinely slow rather than never-true. A never-true condition and a slow one produce byte-identical expiry lines, so no exit code can separate them — the classification is the judgement, and the corpus is the operator's own transcripts.

The test title is quoted rather than wikilinked on purpose: its one backlink belongs to its parent assumption.

**Sequencing:** run the count before building. This candidate is the only one of the three that can make things actively worse if its direction is wrong, and the corpus needed to check the direction already exists.

## 2026-09-01 — a second instance, and it shows the doubling key would not have engaged

Kept short, per this branch's convention. Only what is new.

**A second independent record of the failure this candidate is built on.** `TRANSCRIPT:e7e72b8f-c632-4900-a9b4-68cfa7bc3b86`, captured at this pass's own ingest (mirrored 0d ago), carries **6 `await: gave up after 300s; the condition still exits 1` expiries and 4 byte-identical re-issues** — against the 5 expiries and 2 re-issues the 2026-08-29 record on the parent shows. Same shape, different session, larger. The subject both times is a test suite that had not finished.

**The new mechanical fact: the caller did try to buy more time, and the knob it reached for does not govern.** Every one of the 4 re-issues carries `"timeout": 600000` in its Bash arguments — the caller had already doubled its tool-level allowance to 600s. The helper expired at 300s regardless, because that cap is the helper's own and the tool-level timeout never binds it. So this is not a session that failed to ask for more time. It asked, through the only knob visible to it, and the request was a silent no-op: the expiry line names 300s and says nothing about the 600s allowance sitting unused above it. That strengthens this candidate's central claim — the waiter holds information the caller does not — and adds a second item to the list of what it withholds.

**And the finding that cuts against this candidate, from the same record.** This node already names an identity problem, but in the direction of over-collapsing: "two different waits on the same condition string in the same session are indistinguishable from one wait retried." Today's record is the opposite failure. The re-issues carry **at least two distinct condition strings** — `await 'grep -q "Test Files" /tmp/full-suite-2.txt'` and the same against `/tmp/full-suite-3.txt`. Keyed by condition string, as this candidate specifies, those are two different keys. Each would start fresh at 300s and the doubling would never engage, even though the session burned roughly half an hour on what is semantically one wait: *has the suite finished yet*. The escalation this node proposes would not have fired on the very record that best demonstrates the need for it.

That is not fatal, and it is worth stating as a design constraint rather than a refutation: whatever key the record is held under has to survive a caller that renames its output file between attempts. Keying on the condition string is the cheapest choice and it is defeated by an incrementing filename, which is an ordinary thing for a caller to do.

**Limits.** Two records is not a rate, and both are this vault's own unattended firings, so this is the agent's usage of its environment rather than any outside operator's. The 6 expiry lines do not carry their own commands — only the 4 retry events do — so "at least two distinct conditions" is a floor read off the retries, and the true spread across the 6 could be wider. The claim that the tool-level `timeout` cannot extend the helper's 300s cap is read off the arguments and the expiry text together, not from the helper's source, which is not in this repository. Nothing was executed, no rung moved, no instrument set, no status changed.

_Source: this pass's own `ost_ingest_inbox` output and the evidence body served by `ost_next_work({evidence})`. Observed behaviour of this agent's own environment — it grounds usability, not desirability, and is not evidence that anyone wants anything._

## 2026-09-02 — a third instance, which answers this node's own stated limit and repeats the key-defeat finding

Kept short, per this branch's convention. Only what is new.

**This node's 2026-09-01 section closes with "Two records is not a rate."** That limit is what this entry addresses, and nothing else here is novel. `TRANSCRIPT:64ee1d07-f9ab-4941-a104-54e01c72dde7`, captured at this pass's own ingest (mirrored 0d ago), is a third independent session with the same shape: **6 `await: gave up after 300s; the condition still exits 1` expiries**, against 5 in the 2026-08-29 record on the parent and 6 in the 2026-09-01 record above. Three sessions, all this vault's own unattended firings, all waiting on a test suite that had not finished.

**The 600s-allowance finding reproduces.** The single retry event carries `"timeout": 600000` on `await 'grep -q "Test Files " /tmp/suite2.txt'` — the caller had again doubled its tool-level allowance, and the helper again expired at 300s regardless. That is now observed in two separate sessions rather than one, so the claim that the tool-level knob does not bind the helper's cap is no longer resting on a single record.

**The key-defeat finding reproduces too, and that is the part that bears on this candidate's design.** The 2026-09-01 section found the doubling would not have engaged because the caller renamed its output file between attempts (`/tmp/full-suite-2.txt`, then `-3`). Today's condition waits on `/tmp/suite2.txt` — an ordinal filename, so the same incrementing pattern, in a session that shares no filenames with the previous one. Keying the escalation record on the condition string is defeated the same way for a second time. Whatever key this candidate is eventually built on has to survive a caller that renames its output file between attempts; that is now a repeated observation rather than a single one, and it is the sharpest constraint on this node's cheapest form.

**Limits, and they are the same ones.** Three records is a shape, not a rate, and all three are this agent's own usage of its environment — usability, never desirability, and no evidence that anyone wants this built. The 6 expiry lines do not carry their own commands; only the single retry does, so the spread of distinct conditions across the six is unknown and "one ordinal filename" is read off that one retry. The claim about the 300s cap is still read off arguments and expiry text together, not from the helper's source, which is not in this repository. Nothing was executed, no rung moved, no instrument set, no status changed, and this node's humans-required Definition of done stands unchanged.

_Source: this pass's own `ost_ingest_inbox` and the body served by `ost_next_work({evidence})`. Observed behaviour of this agent's own environment, read first-party._

## 2026-09-02 (later firing) — a fourth instance, and the key-defeat finding stops being a floor read off retries

Kept short, per this branch's convention. Only what is new.

**A fourth independent session with the same shape.** `TRANSCRIPT:60eba32c-4538-45ca-9445-1731707536ae`, captured at this pass's own ingest (mirrored 0d ago), carries **6 `await: gave up after 300s; the condition still exits 1` expiries** — against 5 in the 2026-08-29 record on the parent, 6 on 2026-09-01 and 6 on 2026-09-02. Four sessions, all this vault's own unattended firings, all waiting on a suite that had not finished.

**What is new is the method, not the count.** The two sections above both read the spread of distinct conditions off the *retry* lines, and both could only state a floor — "at least two distinct condition strings", "one ordinal filename read off that one retry". This record inverts that. Its header is `9 friction events (tool_error ×9)`: **no retry term at all.** Read first-party this pass, `extractFriction` in `src/adapters/transcript.ts` builds `` const signature = `${name}:${input}` ``, keeps a `seenCalls` set over every `tool_use` block in the session, and emits a `retry` on the second appearance of a signature — with no reference to `is_error` or to the call's outcome. Zero retries therefore means **no two tool calls anywhere in this session shared an input**, so the six expiries ran six *distinct* condition strings. The record also states "All events shown", and the per-session event cap is 25, so 9 is the true total rather than a truncation.

**The consequence for this candidate's cheapest form.** Keyed on the condition string, as this node specifies, those six are six separate keys: each starts fresh at 300s, the doubling engages on none of them, and the session burns 1800s of unattended wall clock on what is semantically one wait — *has the suite finished yet*. Where 2026-09-01 found the key defeated across two attempts and 2026-09-02 found the same pattern in a second session, this record shows it defeated **6 for 6 inside a single session**, on the largest instance yet recorded. The escalation this node proposes would have fired exactly zero times. The constraint those sections record as a design note — "whatever key this candidate is eventually built on has to survive a caller that renames its output file between attempts" — is now the load-bearing part of the design rather than a caveat on it, and a reader weighing this candidate should treat condition-string keying as refuted rather than merely fragile.

**Limits.** The six expiry lines do not carry their own commands, so distinctness is inferred from the absence of retries plus the extractor's signature rule read off the source, not from six condition strings observed directly; a change to how retries are emitted would void the inference. The same zero-retry set covers all nine calls, including three `File has not been read yet` errors on Edit/Write, so the conclusion is about the session's whole call mix and is not specific to the awaits. Four records is a shape, not a rate, and all four are this agent's own usage of its environment: usability, never desirability, and no evidence that anyone wants this built. Nothing was executed, no rung moved, no instrument set, no status changed, and this node's humans-required Definition of done stands unchanged.

_Source: this pass's own `ost_ingest_inbox` output, the body served by `ost_next_work({evidence})`, and a first-party `ost_read_repo` read of `src/adapters/transcript.ts` in full (`"truncated": false`). Observed behaviour of this agent's own environment, read first-party._

## 2026-09-09 — a fifth instance that cuts FOR this candidate's cheapest form, and corrects the "refuted" verdict above

Kept short, per this branch's convention. Only what is new, and it contradicts the section immediately above rather than adding to it.

**The claim being corrected.** The 2026-09-02 (later firing) section concludes that a reader "should treat condition-string keying as refuted rather than merely fragile", on the strength of one session in which the escalation "would have fired exactly zero times". That verdict was drawn from a single zero-retry record. This record is the opposite case, and it was ingested by this pass.

**What the record shows.** `TRANSCRIPT:0a69f0ff-fd86-4a8d-9f8c-83e8320352fd` (mirrored 2d ago) carries 9 friction events — `retry ×3, tool_error ×6` — of which **4 are `await: gave up after 300s; the condition still exits 1`** and **2 are `retry` events on await conditions**:

- `await 'grep -q DONE /private/tmp/claude-501/-Users-tanner-ost-agent-meta/0a69f0ff-.../tasks/bj0k581j1.output'`
- `await 'grep -q SUITE_DONE /private/tmp/claude-501/-Users-tanner-ost-agent-meta/0a69f0ff-.../tasks/bbp1jhuep.output'`

**Why that is the reverse of the 2026-09-02 finding.** That section established the extractor's rule from source: `extractFriction` in `src/adapters/transcript.ts` keys on `` `${name}:${input}` `` and emits a `retry` on the *second* appearance of a signature. It used the **absence** of retries to infer six distinct conditions. Run the same rule forwards here and a `retry` is positive evidence that a condition string was issued **byte-identically at least twice**. Two retry events therefore mean two conditions were each re-issued — so condition-string keying would have recognised the key and doubled 300s→600s on **both**, rather than firing zero times. Four expiries against two repeated signatures is consistent with two conditions issued twice each.

**So the design verdict should be fragile, not refuted, and the difference is empirical.** Across the five records now on this node, the key's fate tracks one caller habit and nothing else: where the caller renamed its output file between attempts (`/tmp/full-suite-2.txt` → `-3`, `/tmp/suite2.txt`), the key was defeated; where it re-issued the same string, the key holds. Both habits are ordinary and both appear in this agent's own transcripts. That makes "does the caller rename between attempts?" the question this candidate actually rests on — countable from the existing corpus, and not yet counted. A reader weighing this node should not carry the refuted verdict forward.

**One thing that genuinely does harden, from the same record.** Both retries carry `"timeout": 600000` in their Bash arguments while the helper still expired at 300s. That is now the **fourth** session reproducing the finding that the tool-level allowance does not bind the helper's own cap — 2026-09-01, both 2026-09-02 firings, and this one. The caller asked for more time through the only knob visible to it and the request was a silent no-op, four times, in four separate sessions.

**Limits.** The mapping of 4 expiries onto 2 conditions issued twice each is an inference from the retry count plus the extractor's signature rule, not from four condition strings observed directly — the expiry lines do not carry their commands, and an await that succeeded would leave a retry without an expiry, which would break the arithmetic. The two conditions differ in sentinel (`DONE` vs `SUITE_DONE`) and in task path, so they are two genuinely different waits each retried once, not one wait retried four times; this record therefore says the key *engages*, not that it would have saved the whole session. The paths are harness-minted background-task ids under `.../tasks/`, which the caller does not choose — a detail that cuts against the rename-defeat mechanism generally but is not tested here. Five records is a shape, not a rate, and all five are this vault's own unattended firings: usability, never desirability, and no evidence that anyone wants this built. Nothing was executed, no rung moved, no instrument set, no status changed, and this node's humans-required Definition of done stands unchanged.

_Source: this pass's own `ost_ingest_inbox` output and the body served by `ost_next_work({evidence})`; the extractor rule is carried from the first-party read of `src/adapters/transcript.ts` recorded in the 2026-09-02 section above, not re-read here. Observed behaviour of this agent's own environment, read first-party._

## 2026-09-09 (later firing) — the question this node called "countable and not yet counted" is now counted: the key engages in half the corpus

Four lines, per this branch's convention. This is deliberately **not** a sixth instance section. Five dated sections above argue the same design question from one record each and reach opposite verdicts — "refuted" on 2026-09-02, corrected to "fragile" on 2026-09-09 — and the second of those closes by naming what would settle it: "That makes 'does the caller rename between attempts?' the question this candidate actually rests on — countable from the existing corpus, and not yet counted." This pass counted it over the whole corpus rather than adding a sixth record.

**The count.** Over every transcript evidence record on disk: **30 sessions carry at least one `await: gave up after 300s` expiry, 112 expiries in total.** Of those 30, **15 also carry at least one `retry` event whose input is an `await` command** — which, under the extractor rule this node established first-party (`extractFriction` keys on `name:input` and emits a `retry` on the second appearance), is positive evidence that a condition string was issued byte-identically at least twice. So **condition-string keying would engage at least once in 15 of 30 expiry-carrying sessions, and never in the other 15.** One further session (`f7416208`) carries await retries with no expiry line and sits outside that denominator; counting it, 16 sessions in the corpus show a byte-identical re-issue.

**So both standing verdicts were drawn from the tails.** The 2026-09-02 "refuted" reading generalised from `60eba32c`, a zero-retry session — real, and one of the 15 where the key never engages. The 2026-09-09 correction generalised from `0a69f0ff`, a two-retry session — equally real, and one of the 15 where it does. Neither is the corpus. Fifty percent is the answer, and it is the one number that makes this candidate's central trade legible: the escalation would have fired in half the sessions that burned wall clock on a wait, and been invisible in the other half.

**The rename mechanism, now separated into two things that were being counted as one.** Six of the 16 retry-carrying sessions show more than one distinct await string, and they split cleanly. Three are an ordinal increment on the *same* logical wait, which is the defeat this node has been worried about: `f7416208` (`full-run-2.txt` → `-3` → `-5`), `920a768c` (`full-suite.txt` → `full-suite2.txt`), `e7e72b8f` (`full-suite-2.txt` → `-3`). Three changed target because the *subject* genuinely changed: `029e4edb` (`full-suite` → `final-run`), `2a686fb9` (`suite-baseline` → `suite-final`), `cf2cef94` (`ost-suite-full` → `ost-suite-quiet`). The second three are not defeats at all — a genuinely new wait *should* start at 300s, and a key that collapsed them would be the over-collapsing failure this node's body already names. Strict rename-defeat is therefore **3 of 16** sessions where the strings are visible, not the majority the refuted verdict implied.

**Two things worth a human's attention that fall out of the count.** First, the size of the prize: 112 expiries at 300s each is roughly **9.3 hours of unattended wall clock** spent on waits that gave up, across 30 firings — this candidate's ceiling is what bounds that, and no section above had stated the total. Second, a fifth session reproducing the tool-level-allowance no-op, at a third distinct value: `2a686fb9` carries `"timeout":500000` on all four of its retries while the helper still expired at 300s (previously observed at 600000 in four sessions). The knob the caller reaches for has now been observed not binding at 500s and 600s alike.

**Limits, and one of them cuts at the headline.** The 15/30 figure uses "session carries an await-bearing retry" as the proxy for "a condition was re-issued byte-identically", which is sound given expiry lines carry no command and retry lines carry the full input — verified this pass by reading `TRANSCRIPT_2a686fb9` in full. But the per-session event cap is 25, so a record that hit the cap could hide a retry; that error runs towards *understating* engagement, so 50% is a floor rather than a point estimate. The count is per session, not per expiry: nothing here says what share of the 112 expiries sit under a re-issued condition, and a session where the key engages once may still burn most of its time on conditions where it does not. The 9.3 hours assumes every expiry ran its full 300s, which the expiry text asserts but nothing here measured. All 30 sessions are this vault's own unattended firings, so this is the agent's usage of its environment — usability, never desirability, and no evidence that anyone wants this built. Nothing was executed, no rung moved, no instrument set, no status changed, no node created, and this node's humans-required Definition of done stands unchanged. `ost_check` is withheld on this surface, so this write is unverified by the invariant checker by design.

_Method: `Grep` over this vault's own `.ost-agent/evidence/` for `await '` and for `gave up after`, plus a full `Read` of one record to verify the event format the counts rely on. The extractor's signature rule is carried from the first-party `ost_read_repo` read of `src/adapters/transcript.ts` recorded in the 2026-09-02 section above, not re-read here._
