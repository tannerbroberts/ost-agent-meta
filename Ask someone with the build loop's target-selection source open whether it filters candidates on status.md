---
type: AssumptionTest
source: 'agent-ideated:2026-08-19-unattended-sweep'
created: '2026-08-19'
evidence: assertion
lane: humans-required
---
#AssumptionTest #unvalidated #evidence/assertion

Threshold: the answer is a direct yes/no plus the file/line, e.g. "no, the candidate query only checks `hasInstrument` and `permitCleared`" or "yes, it checks `status !== 'deferred'` but the check happens before the human's edit lands." Either answer settles which of the three sibling solutions is worth building.

A person outside the building is the measurement here: Answering this requires reading the build loop's source in the product repository (a separate checkout from this vault); this unattended maintenance pass holds no repo-read grant, so a person with that access must check and report back.

## Issues
- 2026-08-22 2026-08-22 (unattended sweep, repo sight held) — THE REPOSITORY ANSWERS THIS, and the answer is the first of the two the node offered: "no, the candidate query only checks hasInstrument and permitCleared." The node's stated reason for the humans-required lane is "this unattended maintenance pass holds no repo-read grant, so a person with that access must check and report back." That reason is now false — this pass held `ost_read_repo` and read the source. ANSWER WITH FILE AND FUNCTION: target selection is `buildableSolutions` in `src/eval/buildable.ts`. Its whole loop body is `if (n.layer !== "Solution") continue;` followed by `permitFrom(index, n.title)`, and it pushes any solution whose permit is cleared. It NEVER reads `n.status`. `permitFrom` checks only: the node is a Solution; it has tests beneath it; some test carries an `instrument:`; and some instrumented test satisfies `observedRed(t) && !observedGreen(t)`. Status is absent from every one of those clauses. So a solution marked `deferred` that still carries a red instrument IS returned as a build candidate. THE ASYMMETRY THAT EXPLAINS HOW THIS WENT UNNOTICED: the sibling function `solutionsMissingInstruments`, in the SAME FILE, does filter on status — it opens with `if (n.status === "deferred") continue;` and `if (trustsShippedStatus(n)) continue;`, and its docstring argues both exclusions at length. Two adjacent functions over the same node set, one status-aware and one not, is why reading either one alone gives the wrong impression of the other. CORROBORATION ALREADY ON THIS TREE: the test node "buildableSolutions drops a deferred solution that still carries a red instrument" asserts exactly the behaviour this read finds absent — so that test is describing unbuilt behaviour and should be red today, which is what an instrument on it is for. SECOND SELECTION PATH, for completeness: `examples/automation/build-pass.sh` also builds candidates by grepping `^## Results`, finding the linking Solution, and running `ost-agent gate <sol>`. That path applies no status filter either. WHAT IS STILL A HUMAN'S: re-laning this test. The stated blocker is gone, but `ost_flag_humans_required` only ever removes work from compute's reach and the permissive direction is `ost-agent lane "Ask someone with the build loop's target-selection source open whether it filters candidates on status" --set compute-only`. NOT ACTING: no lane changed, no result recorded, no instrument set. Source: first-party `ost_read_repo` of `src/eval/buildable.ts` and `examples/automation/build-pass.sh`. Grounds feasibility only; nothing executed; rung unchanged at the assertion floor.
